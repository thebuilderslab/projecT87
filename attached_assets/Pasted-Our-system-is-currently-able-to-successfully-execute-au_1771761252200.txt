Our system is currently able to successfully execute auto-supplies and auto-buys. The base smart contracts and environment variables are functioning correctly. I need you to audit the backend Python/TypeScript code and database logic and propose concrete code-level fixes for two issues, using a comparison against our old, working “developers” setup.

Repo layout / comparison target

Current code: <path or branch, e.g. ./backend, branch main>.

Old working developers setup: <path or branch, e.g. ./backend_legacy or branch developers-legacy>.

When you say “compare”:

Diff relevant services, models, and strategy engine files between these two versions.

Identify logic that existed in the old setup but is missing or changed in the current one.

ISSUE 1: Strategy sequence fails to trigger after auto-supply
Goal: After a successful auto-supply for a managed wallet, the Macro → Micro → Growth → Capacity strategy pipeline should automatically execute if all readiness conditions are met.

Please:

Map the current readiness / monitoring logic

Locate and summarize the monitoring loop that watches Aave and decides when to call _process_managed_wallet_sync and/or validate_full_automation_ready.

List the current conditions that must be true for a wallet to be considered “ready for strategy execution” (health factor ranges, balances, allowances, flags, etc.).

Compare against the old developers setup

For the same functions and services in the old setup, list any conditions, checks, or calls that have been removed or changed (especially around the old “15-token approval” check).

Explicitly state whether the removal of that check or any related condition now prevents _process_managed_wallet_sync or validate_full_automation_ready from ever evaluating to “ready”.

Borrowing architecture sanity check (delegated vs pull)

We now use delegated_borrow_to_bot so DAI lands directly in the bot’s wallet.

Audit the strategy execution path for:

Any remaining pull_token_from_user calls that should now operate on the bot wallet.

Missing Uniswap router approvals or ERC-20 approvals on the bot wallet that could cause a silent failure and abort the sequence.

For each problematic call, specify the file, function, and exact change needed (e.g. “change user_address to bot_address when building this tx”, “add approval step X before swap Y”).

Health factor / safety checks

Compare the pre-flight health factor and safety checks used before strategy execution in the old setup vs the current one.

Identify any checks that were removed when the Delegation Manager was deprecated and explain how that might cause the bot to skip execution.

Deliverable for Issue 1

Output a structured list (e.g. numbered bullets) of:

Each missing or altered logic block that can prevent strategies from running.

For each item: file path, function name, one-paragraph explanation, and a proposed code change (pseudo-code or patch).

If possible, add or point to a test (unit/integration) that can reproduce the failure and confirm the fix.

ISSUE 2: Wallet reset & ghost state persistence
Problem: Even after revoking permissions and disconnecting, when wallet 0xC3C2a5b9338B175dC58658D529d51589d73B8cb9 reconnects, it still shows old API keys (status REVOKED) and old activity logs.

Please:

Map current persistence model

Identify the DB tables (SQLite/PostgreSQL) that store:

API keys and their status.

Activity logs.

Wallet-to-account or session mappings.

Explain how these tables link to a wallet public address.

Current “disconnect / revoke” behavior

Find the endpoints / functions that handle:

“Disconnect wallet” from the frontend.

“Revoke” or delete API keys.

Describe precisely what they do in the database (e.g., set a revoked_at timestamp, keep logs, clear sessions, etc.).

Explain why, under the current logic, the UI still sees old API keys and logs on reconnect (e.g., queries ignore revoked_at, logs are always fetched by address, etc.).

Design a true “Hard Reset / Wipe Account”

Propose a dedicated function/endpoint (e.g. POST /wallet/hard-reset) that:

Given a wallet address, deletes or archives:

API keys.

Activity logs.

Any session/state rows so that the next connection behaves like a brand-new user.

Takes into account whether there is any compliance/audit requirement (if so, use soft-delete with a flag and exclude wiped data from normal queries).

Provide concrete code-level changes: which models to update, which queries to modify, and how to ensure the frontend sees a clean slate after a hard reset.

Deliverable for Issue 2

Output:

A short explanation of the current persistence behavior for that wallet address.

A description + pseudo-code or patch for the new hard-reset behavior.

Any necessary changes to the API / frontend contract (e.g., new endpoint to call on “Hard Reset”).

Final output format

Section 1: Findings & fixes for Issue 1 (structured list with file/function and proposed changes).

Section 2: Findings & fixes for Issue 2 (DB model summary + reset design).

Section 3: Suggested tests to verify both issues are resolved.

Do not just describe problems; propose concrete code-level changes and, where possible, tests that prove the fix.

