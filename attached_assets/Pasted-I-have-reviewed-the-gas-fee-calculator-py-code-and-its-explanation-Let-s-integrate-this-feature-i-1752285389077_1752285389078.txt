I have reviewed the `gas_fee_calculator.py` code and its explanation. Let's integrate this feature into `arbitrum_testnet_agent.py`.

Please make the following modifications:

**Part 1: Initialize the Gas Calculator**

1.  **Import:** At the top of `arbitrum_testnet_agent.py`, add an import statement for `ArbitrumGasCalculator` from `gas_fee_calculator.py`.
    ```python
    from gas_fee_calculator import ArbitrumGasCalculator
    ```
2.  **Instantiate:** In the `__init__(self)` method of the `ArbitrumTestnetAgent` class, add a line to initialize the gas calculator. Place it right after where `self.uniswap` or `self.aave` are initialized (or within the `initialize_integrations` method, if that's more appropriate, but `__init__` for `self.gas_calculator = ArbitrumGasCalculator()` is better if it doesn't rely on `w3` being fully connected yet.) Let's place it in `initialize_integrations` for simplicity and connection reliance.

    In the `initialize_integrations(self)` method, inside the `try` block where `self.aave` and `self.uniswap` are set up, add:
    ```python
                # Initialize Gas Calculator
                self.gas_calculator = ArbitrumGasCalculator()
                print("⛽ Initialized Gas Calculator.")
    ```

**Part 2: Apply Gas Optimization to Token Approvals (in `initialize_integrations`)**

In the `initialize_integrations` method, locate the existing token approval calls (for USDC, WBTC, WETH, DAI). For each `self.aave.approve_token` call, you need to fetch the gas price and pass it.

Modify each `approve_token` call to:
* Get gas parameters using `gas_params = self.gas_calculator.calculate_transaction_fee('approve_token', speed='market')`.
* Pass the `gas_price_wei` from `gas_params` to the `approve_token` method. Assume the method accepts a `gas_price` parameter.

    Example modification for USDC approval (apply similarly to WBTC, WETH, DAI approvals):
    ```python
                print("🚀 Approving USDC for Aave Pool...")
                gas_params = self.gas_calculator.calculate_transaction_fee('approve_token', speed='market')
                if gas_params:
                    self.aave.approve_token(
                        token_address=self.usdc_address,
                        spender_address=self.aave.pool_address,
                        amount_in_human_readable=float('inf'),
                        gas_price=gas_params['gas_price_wei'] # Pass the optimized gas price
                    )
                    print(f"✅ Approved USDC for Aave Pool with gas: {gas_params['gas_price_gwei']} Gwei.")
                else:
                    self.aave.approve_token( # Fallback if gas params not obtained
                        token_address=self.usdc_address,
                        spender_address=self.aave.pool_address,
                        amount_in_human_readable=float('inf')
                    )
                    print("✅ Approved USDC for Aave Pool (using default gas).")
    ```
    *Apply this pattern to all `approve_token` calls in `initialize_integrations`.*

**Part 3: Apply Gas Optimization to Transactions (in `run_real_defi_task`)**

In the `run_real_defi_task` function, modify every transaction call (`self.aave.borrow`, `self.aave.supply`, `self.uniswap.swap_tokens`) to use the gas calculator.

For each transaction, you need to:
* Determine the correct `operation_type` string (e.g., 'aave_borrow', 'aave_supply', 'uniswap_swap').
* Get gas parameters using `gas_params = self.gas_calculator.calculate_transaction_fee('[OPERATION_TYPE]', speed='market')`.
* Pass the `gas_price_wei` from `gas_params` to the respective method. Assume these methods (`borrow`, `supply`, `swap_tokens`) accept a `gas_price` parameter.

    Example for Borrow (apply similarly to all supply and swap calls):
    ```python
            # Action 1: Borrow 6 USDC
            try:
                print("➡️ Action 1: Borrowing 6 USDC...")
                gas_params = self.gas_calculator.calculate_transaction_fee('aave_borrow', speed='market')
                if gas_params:
                    tx_hash = self.aave.borrow(
                        asset_address=self.usdc_address,
                        amount_in_human_readable=6.0,
                        interest_rate_mode=2,
                        gas_price=gas_params['gas_price_wei'] # Pass the optimized gas price
                    )
                    print(f"✅ Borrowed 6 USDC. Tx: {tx_hash.hex()} with gas: {gas_params['gas_price_gwei']} Gwei.")
                else:
                    tx_hash = self.aave.borrow( # Fallback if gas params not obtained
                        asset_address=self.usdc_address,
                        amount_in_human_readable=6.0,
                        interest_rate_mode=2
                    )
                    print(f"✅ Borrowed 6 USDC. Tx: {tx_hash.hex()} (using default gas).")
                time.sleep(5) # Give time for transaction to confirm
            except Exception as e:
                print(f"❌ Error borrowing USDC: {e}")
                # ... (rest of the error handling) ...
    ```
    *Apply this pattern for all `self.aave.supply` calls (use `'aave_supply'` for `operation_type`) and all `self.uniswap.swap_tokens` calls (use `'uniswap_swap'` for `operation_type`).*

Ensure all existing print statements are updated to reflect the new `gas_price_gwei` information where applicable. If any method (like `approve_token`, `borrow`, `supply`, `swap_tokens`) doesn't inherently accept a `gas_price` parameter, please add a comment indicating that the method signature would need modification within `AaveIntegration` or `UniswapIntegration`.
