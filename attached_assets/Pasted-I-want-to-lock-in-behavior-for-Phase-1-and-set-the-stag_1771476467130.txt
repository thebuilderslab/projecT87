I want to lock in behavior for Phase 1 and set the stage for Phase 2.

Phase 1 – Desired Behavior (what should exist now)
When a user connects their wallet to /app, they should get:

Read‑only DeFi view

Aave V3 health factor, total collateral, total debt, and net worth for their wallet (via fetch_aave_position_for_wallet).

No contract approvals, no on‑chain actions executed on their behalf.

Data & analytics

Full access to their filings, pipeline, and analysis views from Postgres for their selected towns.

REAA Command Center + chat assistant using their real DeFi + pipeline context.

Explicit connect / disconnect semantics

Connect: full read‑only view and REAA functionality for that wallet.

Disconnect: REAA Command Center returns to the disconnected state and the autonomous bot is paused for that wallet until they connect again.

To achieve (3) in a clean way, please implement the following.

A. Per‑user bot enable flag
Database + db.py

Add a boolean column to users:

sql
alter table users
  add column if not exists bot_enabled boolean not null default true;
In db.py, add helpers:

python
def set_bot_enabled(user_id, enabled: bool):
    with get_conn() as conn:
        with conn.cursor() as cur:
            cur.execute(
                "update users set bot_enabled = %s where id = %s",
                (enabled, user_id),
            )

def is_bot_enabled(user_id) -> bool:
    with get_conn() as conn:
        with conn.cursor() as cur:
            cur.execute(
                "select bot_enabled from users where id = %s",
                (user_id,)
            )
            row = cur.fetchone()
            return bool(row[0]) if row else False
This is the single source of truth for whether the bot is allowed to act for that user.

B. Connect / disconnect API
In web_dashboard.py:

On connect, enable the bot for that user:

python
@app.post("/api/auth/wallet")
def auth_wallet():
    ...
    user = database.upsert_user(wallet)
    database.set_bot_enabled(user["id"], True)
    token = auth_signer.sign(str(user['id']).encode()).decode()
    ...
Add a disconnect endpoint:

python
@app.post("/api/auth/disconnect")
def disconnect_wallet():
    user_id = get_current_user_id()
    database.set_bot_enabled(user_id, False)
    return jsonify({"status": "ok", "botEnabled": False})
Optionally, a simple status endpoint:

python
@app.get("/api/user/status")
def user_status():
    user_id = get_current_user_id()
    return jsonify({"botEnabled": database.is_bot_enabled(user_id)})
C. Bot/agent respect bot_enabled
Wherever the autonomous agent runs macro/micro/growth/capacity strategies on a per‑wallet basis (or when you extend it to multi‑wallet), add a guard:

python
def run_strategies_for_user(user_id, wallet_address):
    if not database.is_bot_enabled(user_id):
        # Skip autonomous actions for this wallet
        return

    # existing macro/micro/growth/capacity logic here
For now, if the bot only acts on your internal wallet, this just wires the plumbing so it’s ready when you move to multi‑wallet management in Phase 2.

D. Frontend: Disconnect button + full reset
In templates/consumer_dashboard.html:

Add a Disconnect button next to the wallet display:

xml
<button id="disconnectBtn" class="wallet-btn secondary" style="display:none;" onclick="disconnectWallet()">
  Disconnect
</button>
Update authenticateWallet():

js
async function authenticateWallet() {
  // existing connect logic...
  state.authToken = data.authToken;
  state.walletAddress = walletAddress;

  walletBtn.textContent = truncateAddress(walletAddress);
  walletBtn.onclick = null;
  document.getElementById('disconnectBtn').style.display = 'inline-flex';

  // existing DeFi loading + income + pipeline calls
}
Implement disconnectWallet():

js
async function disconnectWallet() {
  if (state.authToken) {
    await api('/api/auth/disconnect', { method: 'POST' });
  }
  fullDisconnect();
}
Ensure fullDisconnect() hides the disconnect button and restores the connect behavior (you already have most of this):

js
function fullDisconnect() {
  state.authToken = null;
  state.userId = null;
  state.walletAddress = null;
  state.chatHistory = [];
  state.chatBusy = false;
  state.defiPosition = null;
  state.safetyScore = 0;
  state.userTownIds = [];
  state.towns = [];

  const walletBtn = document.getElementById('walletBtn');
  const disconnectBtn = document.getElementById('disconnectBtn');
  walletBtn.textContent = 'Connect Wallet';
  walletBtn.onclick = connectWallet;
  if (disconnectBtn) disconnectBtn.style.display = 'none';

  // existing resets: DeFi panel, health ring, income, pipeline, REAA greeting
}
Result:

Connect → bot_enabled = true, full REAA experience for that wallet.

Disconnect → bot_enabled = false, UI returns to “Connect Wallet” and the bot skips that wallet until they connect again.

Phase 2 – Delegated execution (future project)
Separately (Phase 2), I want to implement your Delegation Manager idea:

Delegation Manager contract on Arbitrum where users approve REAA to perform a limited set of Aave actions.

managed_wallets table in Postgres tracking:

wallet, user_id

delegation status (active/revoked/pending)

allowed strategies / limits.

Multi‑wallet agent loop that:

Iterates through managed wallets.

Reads each wallet’s health factor.

Chooses macro/micro/growth/capacity strategy.

Executes via the Delegation Manager contract.

Logs actions to a per‑wallet wallet_actions table.

/app UX:

After connect, a clear “Approve REAA to manage my DeFi strategy” step (with permissions + revoke info).

Command Center showing current strategy mode, last actions, and delegation status.

For now, let’s:

Finish and stabilize Phase 1 with the connect/disconnect behavior above.

Then, for Phase 2, produce:

A short design doc for the Delegation Manager contract (methods/events/safety).

A proposed schema for managed_wallets and wallet_actions.

A step‑by‑step plan for contract deployment, bot integration, and /app UX changes.

Once the Phase 1 disconnect behavior (bot_enabled + /api/auth/disconnect + frontend button) is in place and tested, I’ll start using /app with my own wallet and then we can move on to the Phase 2 design.