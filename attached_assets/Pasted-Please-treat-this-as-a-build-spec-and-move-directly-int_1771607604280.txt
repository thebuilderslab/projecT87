Please treat this as a build spec and move directly into implementation. Do not plan or summarize — implement, test, and show evidence.

Context: Current state
The delegated wallet 0xd60a...5cff (user 22) has:

$110.50 WBTC supplied on Aave as collateral.

Zero debt, HF = 999.99.

delegation_status='active', delegation_mode='full_automation', bot_enabled=true.

But Last Action: ERROR: permission misconfiguration — missing: allowBorrow, allowRepay, allowWithdraw.

This means:

The wallet IS being picked up by get_active_managed_wallets() each cycle.

The strategy engine IS evaluating it.

But it exits immediately at the permission check before any strategy runs.

No Growth, Capacity, Macro, or Micro has fired.

Additionally:

The monitoring loop is evaluating dozens of test/placeholder wallets every cycle (users 9, 11, 12, 25, 51, 52, 57, 58 with addresses like 0xdeadbeef..., 0xfeed0000..., 0x1111..., etc.).

These create log noise and unnecessary RPC/DB writes every cycle.

The connect+sign flow currently only sets allowSupply=true on the DelegationManager. It does NOT set allowBorrow, allowRepay, or allowWithdraw, and does NOT include ERC20 approvals for the required tokens.

Fix 1: Audit and confirm current permission state
Before changing anything, please:

Query the REAADelegationManager contract directly for wallet 0xd60a...5cff and print the current values of:

isActive

allowSupply

allowBorrow

allowRepay

allowWithdraw

Query current ERC20 allowances for that wallet for each of the 5 tokens (DAI, WETH, WBTC, USDC, USDT) to each of the 3 contracts:

REAADelegationManager (0x7427370Ab4C311B090446544078c819b3946E59d)

Aave V3 Pool (0x794a61358D6845594F94dc1DB02A252b5b4814aD)

Uniswap Router (address from agent config)

Show the current defi_positions row for user 22 / wallet 0xd60a...:

collateral, debt, hf, has_active_position, wallet_address

Show the current managed_wallets row for user 22:

delegation_status, strategy_status, delegation_mode, bot_enabled, auto_supply_wbtc

This gives us a baseline before any changes.

Fix 2: Update the connect+sign and activation flow
The connect+sign and delegation activation flow must be updated so that every new wallet that connects and enables Auto-Pilot automatically gets full permissions in a single user-signed transaction or sequence of transactions.

Implement the following in the activation flow (wherever activate_delegation or equivalent runs):

DelegationManager flags

Call REAADelegationManager for the connecting wallet and set:

python
allowSupply = True
allowBorrow = True
allowRepay = True
allowWithdraw = True
isActive = True
If the contract requires a single configureDelegation call with all flags, use that. If it requires individual calls, make all of them before marking the wallet active in the DB.

ERC20 approvals (infinite)

For each of the 5 tokens (DAI, WETH, WBTC, USDC, USDT), require the user wallet to grant infinite approval (type(uint256).max) to each of the 3 contracts:

REAADelegationManager (0x7427370Ab4C311B090446544078c819b3946E59d)

Aave V3 Pool (0x794a61358D6845594F94dc1DB02A252b5b4814aD)

Uniswap Router (address from config)

Total: 15 approvals (5 tokens × 3 contracts). These can be batched into a single multicall if your frontend supports it, or presented as sequential signing steps.

Pre-activation validation

Before setting delegation_status='active' and strategy_status='active' in the DB, run check_user_wallet_approvals() to verify:

All 4 DelegationManager flags are true.

All 15 ERC20 approvals are present (or at least the minimum required for the next strategy cycle).

If anything is missing:

Do NOT set strategy_status='active'.

Set strategy_status='error_permissions'.

Log exactly which flags or approvals are missing.

Surface this in /api/user/status so the dashboard can show the user what still needs to be signed.

DB updates (only after on-chain confirmation)

Once all flags and approvals are confirmed on-chain:

python
delegation_status = 'active'
delegation_mode = 'full_automation'
strategy_status = 'active'
bot_enabled = True
auto_supply_wbtc = True
Fix 3: Manual remediation script for 0xd60a...5cff
Since this wallet was connected before the fix, it cannot use the updated flow retroactively. Please write a one-time remediation script that:

Calls REAADelegationManager and sets all four flags to true for 0xd60a...5cff.

Checks and logs the current ERC20 allowances for that wallet for all 5 tokens to all 3 contracts.

If any allowances are missing or below the required amount, logs exactly which ones need to be signed manually by the user.

After the on-chain calls are confirmed, updates the DB:

python
strategy_status = 'active'
delegation_mode = 'full_automation'
Prints a final summary:

text
Wallet: 0xd60a...5cff
DelegationManager flags: allowSupply=True, allowBorrow=True, 
  allowRepay=True, allowWithdraw=True
ERC20 approvals: [list each token + contract + status]
DB updated: strategy_status=active
Ready for next monitoring cycle: YES/NO
Fix 4: Test wallet cleanup
The monitoring loop currently evaluates dozens of placeholder/test wallets every cycle. Please:

Add an is_test_wallet boolean column to managed_wallets (default false).

Update init_db() to include this column for fresh installs.

Set is_test_wallet=true for all known placeholder addresses currently in the DB, including at minimum:

0xabcdef1234567890abcdef1234567890abcdef12 (user 9)

0x1111111111111111111111111111111111111111 (user 11)

0x0000000000000000000000000000000000000099 (user 12)

0x9e21e28bb0a14bec4b96c107c0532c876425397d (user 25)

0xa1b2c3d4e5f607182930a1b2c3d4e5f60718293a (user 51)

0xdeadbeef00000000000000000000000000000099 (user 52)

0xfeed000000000000000000000000000000000003 (user 57)

0xfeed000000000000000000000000000000000004 (user 58)

Any other addresses that are clearly test/placeholder patterns.

Update get_active_managed_wallets() to exclude rows where is_test_wallet=true.

Confirm by showing the next monitoring cycle log has zero entries for those addresses.

Fix 5: Verify strategy fires after permissions are granted
After Fix 2 and Fix 3 are implemented and the on-chain flags are confirmed:

Restart the bot workflow.

On the next monitoring cycle, confirm logs show for 0xd60a...5cff:

No error_permissions entry.

A strategy evaluation log, e.g.:

text
[Strategy] user=22 wallet=0xd60a... HF=999.99 >= 2.90 → Capacity trigger
[Strategy] Executing delegated borrow DAI amount=X
[Strategy] Step 1/6: DAI supply OK
[Strategy] Step 2/6: DAI→WBTC swap+supply OK
...
Show /api/user/status after the cycle confirming:

json
{
  "strategyStatus": "active",
  "delegationMode": "full_automation",
  "lastStrategyAction": "CAPACITY: borrowed X DAI, 6-step distribution complete",
  "collateral": "<updated value>",
  "debt": "<non-zero DAI>",
  "healthFactor": "<real number, not 999.99>"
}
Confirm the dashboard shows:

Real collateral value (updated after borrow + supply).

Non-zero debt.

HF in a real range (likely 2.5–3.5).

Last Action showing a successful strategy execution, not error_permissions.

Acceptance criteria
This is build-ready when:

The connect+sign flow automatically sets all 4 DelegationManager flags and prompts for all 15 ERC20 approvals before activating a wallet.

0xd60a...5cff has all flags set, all required approvals in place, and strategy_status='active' in the DB.

At least one Capacity or Growth cycle has fired for that wallet with logs showing the 6-step distribution executing.

The monitoring loop no longer processes test/placeholder wallets.

/api/user/status and the dashboard reflect the real, post-strategy state for that wallet.

replit.md is updated to document:

The full connect+sign permission flow.

The 15 required ERC20 approvals.

How is_test_wallet excludes test data from monitoring.