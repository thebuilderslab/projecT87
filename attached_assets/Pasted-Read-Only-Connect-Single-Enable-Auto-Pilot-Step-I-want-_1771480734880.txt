Read‑Only Connect + Single “Enable Auto‑Pilot” Step

I want to lock in this behavior for /app:

Wallet connect is always read‑only by default.

After connect, users see one clear REAA “Enable Auto‑Pilot” step to turn on delegation + WBTC auto‑supply. Nothing is auto‑enabled without that explicit action.

Please implement the following.

1. Read‑only wallet connect (no delegation by default)
Keep POST /api/auth/wallet exactly as it is, but do not automatically call /api/delegation/activate inside it.

After a successful connect:

bot_enabled remains true (for read‑only monitoring).

managed_wallets.delegation_status should be:

'none'

auto_supply_wbtc = false

The user has:

DeFi read‑only view

Filings/pipeline/analysis

REAA chat with their data

No auto‑supply or delegated Aave actions yet.

2. REAA “Enable Auto‑Pilot” step immediately after connect
Frontend:

After connect, once /api/user/status returns, check:

js
if (status.delegation.delegationStatus === 'none') {
  showAutoPilotPrompt();
}
Implement showAutoPilotPrompt() as a centered REAA card/modal over the Command Center:

Title: “Let REAA Manage Your WBTC Auto‑Pilot?”

Body (plain language):

“I can automatically supply up to 80% of your WBTC balance to Aave and monitor your safety band. You can revoke this anytime.”

Buttons:

Primary: “Enable Auto‑Pilot”

Secondary: “Not now”

Button behavior:

“Not now”

Close the modal.

Leave delegationStatus = 'none', autoSupplyWbtc = false.

User stays in read‑only mode.

“Enable Auto‑Pilot”

Trigger the on‑chain Delegation Manager approval flow (if contractDeployed === true):

Show wallet transaction for WBTC delegation approval.

After the tx succeeds, call:

js
await api('/api/delegation/activate', { method: 'POST' });
On success:

Update local state: delegationStatus = 'active', autoSupplyWbtc = true.

Update the left‑panel delegation section to show “Enabled / Revoke”.

Optionally show a small REAA chat message like:

“Auto‑pilot is now enabled. I’ll auto‑supply up to 80% of your WBTC and monitor your Aave safety.”

If contractDeployed === false:

Do not show the auto‑pilot modal.

Keep the current yellow “Delegation Manager contract not yet deployed” message in the DeFi panel and keep the enable button disabled.

3. Backend expectations
/api/auth/wallet stays read‑only (no delegation side effects).

/api/delegation/activate:

Uses get_current_user_id().

Sets delegation_status = 'active' and auto_supply_wbtc = true in managed_wallets.

Records a wallet_actions entry with action_type='delegation_granted'.

/api/delegation/revoke continues to turn off auto‑pilot:

delegation_status = 'revoked', auto_supply_wbtc = false.

Logs delegation_revoked.