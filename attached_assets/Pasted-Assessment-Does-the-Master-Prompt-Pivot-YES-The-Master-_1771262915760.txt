Assessment: Does the Master Prompt Pivot?YES. The Master Prompt represents a significant logic pivot from the system Replit just summarized.Replit's Summary (Phase 1): Describes a system based on Fixed Numbers (Borrow $11.40, Tax $1.20) and File-Based Storage (execution_state.json). It is focused on deploying the current (flawed) prototype.The Master Prompt (Phase 2): Describes a system based on Reverse-Calculated Targets (Profit-First) and Dynamic Polling. It is focused on fixing the engine before deployment.Do you need to provide more context?Yes. You must explicitly tell Replit to PAUSE the deployment/database migration tasks. If you don't, Replit might try to set up a PostgreSQL database for the old, flawed logic instead of rewriting the code first.You need a "Bridge" instruction that says: "Your summary is accurate for the prototype, but we are upgrading the logic to 'Phase 2' BEFORE we deploy. Ignore the VM/Database setup for this step."The Final Master Instructional PromptCopy and paste this entire block into Replit. It includes the "Bridge Context," the Logic, Code Structures, and Documentation requirements.SYSTEM UPGRADE: Phase 2 "Target Profit" Logic & API OptimizationCONTEXT & BRIDGEYour previous summary accurately described the "Phase 1" prototype (Fixed $11.40 borrows, $1.20 tax). However, we are NOT deploying that version.That logic is unsustainable because it forces distributions even when unprofitable. We are immediately upgrading to the "Phase 2: Target Profit Engine" described below.Instruction: IGNORE the VM/Database/Deployment tasks for now.Focus: Solely on refactoring run_autonomous_mainnet.py and the helper logic to implement the "Reverse-Profit" and "Smart Polling" systems.OBJECTIVERefactor the Liability Short (Micro/Macro) functions. Instead of blindly executing on a % drop, the system must work backwards:Define Goal: "I need $10.00 Profit."Calculate: "Market must drop to $X price to generate $10.00 + Fees."Execute: Only trade if that price target is hit.MODULE 1: API Optimization (The "Sentry" System)File: arbitrum_testnet_agent.pyRequirement: Move from constant polling to Dynamic Polling to save API credits.Code Logic:Python# Inside the main monitoring loop
if self.execution_state["status"] == "IDLE":
    polling_interval = 90  # Slow polling (Sentry Mode)
elif self.execution_state["status"] == "SHORT_ACTIVE":
    polling_interval = 15  # Fast polling (Hunter Mode)

time.sleep(polling_interval)
MODULE 2: The "Reverse-Profit" CalculatorFile: liability_short_engine.py (Create or Refactor)Function: _calculate_required_drop(short_size_usd, target_profit_usd)Logic Steps:Estimate Costs:Swap_Entry_Fee: 0.3% of short_size_usd.Swap_Exit_Fee: 0.3% of short_size_usd.Gas_Buffer: Fixed $0.50 (Arbitrum L2 estimate).Gross Revenue Needed: target_profit_usd + Swap_Entry_Fee + Swap_Exit_Fee + Gas_Buffer.Required Drop %: gross_revenue_needed / short_size_usd.Target Price: entry_price * (1 - required_drop_pct).Output: Return target_price and required_drop_pct.MODULE 3: The Execution Loop (The "Round Trip")Function: _execute_liability_short(mode)Step A: The Setup (Open Short)Trigger: Collateral drops > 2% (Micro) or > 5% (Macro).Goal Setting:Target_Wallet_S: $2.00Target_Wallet_B: $2.00Target_Collateral: $6.00Total_Target: $10.00Validation: Call _calculate_required_drop(loan_amount, 10.00).Safety Gate: IF required_drop_pct > 4% (for Micro), ABORT. (Unrealistic target).Action: Borrow ETH $\to$ Swap to DAI $\to$ Supply DAI to Aave (Hold as collateral).State Update: {"status": "SHORT_ACTIVE", "target_price": <calculated_target>}.Step B: The Hunt (Monitor)Loop: Poll every 15s.Condition 1 (Win): Current_Price <= target_price. $\to$ Execute Step C.Condition 2 (Stop Loss): Current_Price >= entry_price * 1.015. $\to$ Close immediately (Absorb loss).Step C: The Close (Distribute)Action: Withdraw DAI $\to$ Swap to ETH $\to$ Repay Loan.Profit Logic:Realized_Profit = Remaining_DAI_Balance.IF Realized_Profit >= Total_Target:Send Target_Wallet_S to Wallet S.Send Target_Wallet_B to Wallet B.Keep remainder as DAI Collateral.ELSE: Log "Trade Breakeven/Loss. No Distribution."MODULE 4: Documentation & LogsConsole Output Requirements:(Visual aid for logs if possible, otherwise text).Log: "📉 SHORT TRIGGERED: Target Net Profit $10.00"Log: "🧮 CALCULATION: Market must drop -2.4% ($1,850) to cover fees + profit."Log: "⏳ HUNTER MODE: Polling every 15s..."Execute this Logic Upgrade immediately. Do not proceed to Database setup until this logic is verified.