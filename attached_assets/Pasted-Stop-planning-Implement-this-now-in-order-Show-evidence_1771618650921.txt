Stop planning. Implement this now in order. Show evidence for each step before moving to the next.

Step 1: Confirm the mismatch and env var availability before touching anything
Run this diagnostic now and print the full output:

python
import os
from web3 import Web3
from eth_account import Account

w3 = Web3(Web3.HTTPProvider(os.environ["RPC_URL"]))

# Check env vars
bot_key = os.environ.get("BOT_PRIVATE_KEY")
deployer_key = os.environ.get("DEPLOYER_PRIVATE_KEY")

print(f"BOT_PRIVATE_KEY set     : {bool(bot_key)}")
print(f"DEPLOYER_PRIVATE_KEY set: {bool(deployer_key)}")
print()

# Derive addresses
bot_wallet = Account.from_key(bot_key).address
print(f"Bot wallet address: {bot_wallet}")

if deployer_key:
    deployer_wallet = Account.from_key(deployer_key).address
    print(f"Deployer address  : {deployer_wallet}")
else:
    print("Deployer key not available in Replit env")
print()

# Query live contract state
CONTRACT_ADDRESS = "0x7427370Ab4C311B090446544078c819b3946E59d"

# Minimal ABI for diagnostic
abi = [
    {"inputs": [], "name": "botOperator",
     "outputs": [{"internalType": "address", "type": "address"}],
     "stateMutability": "view", "type": "function"},
    {"inputs": [], "name": "owner",
     "outputs": [{"internalType": "address", "type": "address"}],
     "stateMutability": "view", "type": "function"},
    {"inputs": [], "name": "paused",
     "outputs": [{"internalType": "bool", "type": "bool"}],
     "stateMutability": "view", "type": "function"},
]

contract = w3.eth.contract(address=CONTRACT_ADDRESS, abi=abi)

current_operator = contract.functions.botOperator().call()
owner = contract.functions.owner().call()

try:
    is_paused = contract.functions.paused().call()
except:
    is_paused = "function not found"

print(f"Contract owner      : {owner}")
print(f"Current botOperator : {current_operator}")
print(f"Bot wallet          : {bot_wallet}")
print(f"Contract paused     : {is_paused}")
print()
print(f"botOperator matches bot wallet: "
      f"{current_operator.lower() == bot_wallet.lower()}")

if deployer_key:
    print(f"Deployer is owner: "
          f"{deployer_wallet.lower() == owner.lower()}")
Print this full output. Do not proceed until this is shown.

Based on the output, one of two paths applies:

Path A: If DEPLOYER_PRIVATE_KEY is available in Replit env
Step 2A: Confirm updateBotOperator is in the ABI

Check delegation_client.py for this entry. If it is not present, add it now:

python
{
    "inputs": [
        {
            "internalType": "address",
            "name": "newBot",
            "type": "address"
        }
    ],
    "name": "updateBotOperator",
    "outputs": [],
    "stateMutability": "nonpayable",
    "type": "function"
}
Step 3A: Create and run scripts/set_bot_operator.py

python
# scripts/set_bot_operator.py
# ONE-TIME SETUP — run from deployer wallet only
# Sets the bot wallet as botOperator on REAADelegationManager
# Required before any user borrow/repay/withdraw can execute

import os
from web3 import Web3
from eth_account import Account

w3 = Web3(Web3.HTTPProvider(os.environ["RPC_URL"]))

CONTRACT_ADDRESS = "0x7427370Ab4C311B090446544078c819b3946E59d"
DEPLOYER_PRIVATE_KEY = os.environ["DEPLOYER_PRIVATE_KEY"]
BOT_PRIVATE_KEY = os.environ["BOT_PRIVATE_KEY"]

deployer = Account.from_key(DEPLOYER_PRIVATE_KEY)
bot_wallet = Account.from_key(BOT_PRIVATE_KEY).address

# Full ABI entry for updateBotOperator
abi = [
    {"inputs": [],
     "name": "botOperator",
     "outputs": [{"internalType": "address", "type": "address"}],
     "stateMutability": "view",
     "type": "function"},
    {"inputs": [],
     "name": "owner",
     "outputs": [{"internalType": "address", "type": "address"}],
     "stateMutability": "view",
     "type": "function"},
    {"inputs": [{"internalType": "address",
                 "name": "newBot",
                 "type": "address"}],
     "name": "updateBotOperator",
     "outputs": [],
     "stateMutability": "nonpayable",
     "type": "function"},
]

contract = w3.eth.contract(address=CONTRACT_ADDRESS, abi=abi)

# Pre-flight checks
current_operator = contract.functions.botOperator().call()
owner = contract.functions.owner().call()

print("=== PRE-FLIGHT ===")
print(f"Contract owner      : {owner}")
print(f"Deployer wallet     : {deployer.address}")
print(f"Current botOperator : {current_operator}")
print(f"Target bot wallet   : {bot_wallet}")
print()

# Safety assertions
assert owner.lower() == deployer.address.lower(), (
    f"ABORT: Deployer {deployer.address} is NOT "
    f"the contract owner {owner}. "
    f"Only the owner can call updateBotOperator."
)

if current_operator.lower() == bot_wallet.lower():
    print("botOperator is already correctly set. No action needed.")
    exit(0)

# Build transaction
print("Building updateBotOperator transaction...")
tx = contract.functions.updateBotOperator(
    bot_wallet
).build_transaction({
    "from": deployer.address,
    "nonce": w3.eth.get_transaction_count(deployer.address),
    "gas": 100000,
    "gasPrice": w3.eth.gas_price,
    "chainId": 42161
})

# Sign and send
signed = w3.eth.account.sign_transaction(tx, DEPLOYER_PRIVATE_KEY)
tx_hash = w3.eth.send_raw_transaction(signed.rawTransaction)
print(f"Transaction sent    : {tx_hash.hex()}")
print(f"Arbiscan            : "
      f"https://arbiscan.io/tx/{tx_hash.hex()}")

# Wait for confirmation
receipt = w3.eth.wait_for_transaction_receipt(tx_hash, timeout=120)
print()
print("=== RESULT ===")
print(f"Status  : "
      f"{'SUCCESS' if receipt.status == 1 else 'FAILED'}")
print(f"Block   : {receipt.blockNumber}")
print(f"Gas used: {receipt.gasUsed}")

# Final confirmation
confirmed = contract.functions.botOperator().call()
print(f"New botOperator     : {confirmed}")
print(f"Correctly set       : "
      f"{confirmed.lower() == bot_wallet.lower()}")
Run the script and print the full output including tx hash and Arbiscan link.

Path B: If DEPLOYER_PRIVATE_KEY is NOT in Replit env
Step 2B: Generate the calldata so it can be run externally

python
from web3 import Web3
from eth_account import Account
import os

w3 = Web3(Web3.HTTPProvider(os.environ["RPC_URL"]))

bot_wallet = Account.from_key(
    os.environ["BOT_PRIVATE_KEY"]
).address

# Encode the calldata for updateBotOperator
calldata = w3.eth.contract(
    address="0x7427370Ab4C311B090446544078c819b3946E59d",
    abi=[{
        "inputs": [{"internalType": "address",
                    "name": "newBot",
                    "type": "address"}],
        "name": "updateBotOperator",
        "outputs": [],
        "stateMutability": "nonpayable",
        "type": "function"
    }]
).encodeABI(fn_name="updateBotOperator", args=[bot_wallet])

print(f"Contract  : 0x7427370Ab4C311B090446544078c819b3946E59d")
print(f"Function  : updateBotOperator")
print(f"Argument  : {bot_wallet}")
print(f"Calldata  : {calldata}")
print()
print("To execute:")
print("Option 1 — Arbiscan Write Contract:")
print("  1. Go to arbiscan.io/address/"
      "0x7427370Ab4C311B090446544078c819b3946E59d")
print("  2. Click Contract → Write Contract")
print("  3. Connect deployer wallet "
      "(0xbbd55BB1...7e27C9c48)")
print("  4. Call updateBotOperator with "
      f"newBot = {bot_wallet}")
print()
print("Option 2 — Run locally with deployer key:")
print("  DEPLOYER_PRIVATE_KEY=<key> "
      "python scripts/set_bot_operator.py")
Print this output and provide it so the transaction can be executed from wherever the deployer key lives.

Step 4: Verify borrow executes after authorization
Once botOperator is confirmed set:

Restart the bot workflow.

Reset cooldown for user 22:

python
database.reset_auto_supply_cooldown(
    22,
    "0xd60acbac798f146e4b7080e9a1f93977200f5cff"
)
Wait for next monitoring cycle and confirm logs show:

text
[Strategy] user=22 wallet=0xd60acbac...
action=STEP1_BORROW → TX CONFIRMED hash=0x...
[Strategy] Step 2/6: DAI supply OK
[Strategy] Step 3/6: DAI→WBTC swap OK
[Strategy] Step 4/6: DAI→WETH swap OK
[Strategy] Step 5/6: DAI→ETH gas OK
[Strategy] Step 6/6: DAI→Wallet_S OK
[Strategy] 6-step distribution COMPLETE
Step 5: Document in replit.md
Add this section:

text
## Contract Admin Setup (One-Time, Deployer Only)

### botOperator Authorization

Before any delegated user wallet can execute 
borrows, repays, or withdrawals, the deployer 
wallet must authorize the bot wallet as 
botOperator on REAADelegationManager.

Contract : 0x7427370Ab4C311B090446544078c819b3946E59d
Network  : Arbitrum Mainnet (chainId 42161)
Function : updateBotOperator(address newBot)
Access   : onlyOwner

Run once:
  python scripts/set_bot_operator.py

OR via Arbiscan Write Contract:
  Connect deployer wallet and call 
  updateBotOperator with the bot wallet address.

This is required ONCE and does not need to be 
repeated when new users connect. It is a 
contract-level configuration, separate from 
per-user approveDelegation() calls.

Functions unblocked after this is set:
  - autoSupplyWBTC
  - executeSupply
  - executeBorrow
  - executeRepay
  - executeWithdraw
Acceptance criteria
Done when:

contract.functions.botOperator().call() returns the bot wallet address.

Script runs with Status: SUCCESS and Correctly set: True.

Capacity borrow transaction confirms on-chain without reverting.

Full 6-step distribution completes for user 22 with all steps logged.

replit.md has the Contract Admin Setup section.

Dashboard shows real HF, non-zero debt, successful last strategy action.