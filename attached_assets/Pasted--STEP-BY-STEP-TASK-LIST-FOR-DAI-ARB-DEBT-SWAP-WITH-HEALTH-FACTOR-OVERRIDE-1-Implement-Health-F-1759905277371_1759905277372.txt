📋 STEP-BY-STEP TASK LIST FOR DAI→ARB DEBT SWAP WITH HEALTH FACTOR OVERRIDE

1. Implement Health Factor Override Logic

Modify the minimum health factor enforcement in the transaction safety checker to allow an override parameter for this run (target: 1.3 instead of the default 1.5).

Pass a min_health_factor_override param (value: 1.3) through all relevant validation and execution modules.

Add explicit logging of OVERRIDE_REASON: user-approved manual DAI→ARB swap.

2. Dry-Run Validation (Simulation)

Fetch all live wallet/account stats: collateral, DAI debt, current health factor.

Simulate the swap for 10 DAI (borrowed) to ARB, using up-to-date oracle prices and ParaSwap or Uniswap routing.

Ensure account health factor after swap remains ≥1.3 at all times.

Run preflight validation (eth_call simulation).

Abort immediately if simulation shows HF <1.3, log reason.

Log expected transaction cost and route.

3. Controlled Execution (Mainnet)

Initiate the debt swap:

Repay 10 DAI borrowed debt using ParaSwap adapter or equivalent.

Borrow ARB equivalent to $10 via Aave Pool, using credit delegation/signature as required.

Swap is atomic—must be "all-or-nothing."

Monitor health factor in real time during execution:

Abort if HF approaches <1.25 (emergency stop).

Enforce execution timeout: abort if mainnet tx takes >60s.

Log all tx hashes, gas used, and confirmation status.

4. Post-Execution Verification & Documentation

Fetch updated account state:

Confirm DAI debt reduced by ~10.

Confirm ARB debt increased by $10.

Confirm HF ≥1.3 after execution.

Confirm ARB is now shown in borrowed assets (not just wallet/holdings), along with remaining DAI debt.

Archive all logs and override usage to system history for audit and compliance.

If any dust or errors remain, document next steps for sweep or resolution.

5. Emergency Revert & Safety

Document a revert or fallback plan if the system encounters any error or gets stuck during execution.

Roll back partial changes if, despite checks, state diverges or verification fails.

REQUEST:

List and confirm availability of all scripts, modules, and configs involved in this debt swap sequence (integration, monitoring, execution, validation, logging).

If any blocking bugs, missing parameters, or implementation gaps are identified during the first attempt, document them clearly for prompt correction.