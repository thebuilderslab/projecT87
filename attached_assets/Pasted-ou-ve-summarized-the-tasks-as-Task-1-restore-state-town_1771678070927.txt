ou’ve summarized the tasks as:

- Task 1 – restore state.towns in fullDisconnect and wrap authenticateWallet loads in await Promise.all.
- Task 2 – update USDC meter markup/CSS and add USDC text in Active Wealth.
- Task 3 – update replit.md and verify.

Now implement them. Do not reply with another plan.

1) fullDisconnect + authenticateWallet

- Paste the current fullDisconnect() implementation.
- Update it so it clears walletAddress, authToken, userId, towns, all DeFi/dashboard state, and localStorage/sessionStorage keys used for wallet persistence. Paste the final version.

- Paste the current authenticateWallet()/connectWallet() function(s).
- Refactor so that, after setting state.authToken and saving to localStorage, you do:

  ```javascript
  try {
    await Promise.all([
      loadDefiPanel(),
      loadTowns(),
      loadRecentPayouts(),
      // other first-load calls
    ]);
  } catch (err) {
    console.error('Initial dashboard load failed', err);
  }
Show the updated function code, not just the diff.

USDC text + meter

Show the JSON shape you use for Command Center/DeFi that includes USDC; if missing, add:

json
{
  "usdc": { "walletBalanceUsd": 0, "targetUsd": 0 }
}
Paste the HTML for the “Active Wealth” panel and add:

xml
<div class="active-wealth-row">
  <span>USDC (wallet)</span>
  <span id="usdcWalletText">$0.00</span>
</div>
Paste the JS that renders it:

javascript
function renderActiveWealth(data) {
  const usdc = data.usdc;
  document.getElementById('usdcWalletText').textContent =
    formatCurrency(usdc.walletBalanceUsd || 0);
}
Paste the USDC meter HTML and update to:

xml
<div class="usdc-meter">
  <div class="usdc-meter-label">
    USDC → WALLET
    <span id="usdcMeterValue">$0.00 / $0.00</span>
  </div>
  <div class="usdc-meter-bar">
    <div id="usdcMeterFill"></div>
  </div>
</div>
Paste the exact CSS you apply for .usdc-meter, .usdc-meter-label, .usdc-meter-bar, #usdcMeterFill and the JS renderUsdcMeter(data) that sets text and width.

Docs + verification

Append to replit.md a short “Wallet connection & USDC meter” section and paste that text.

Run a manual test: disconnect → refresh → connect wallet. Paste a short log excerpt showing:

no 401s during initial load,

Active Wealth shows USDC (wallet) with a value,

the Command Center meter displays $X / $Y and the bar fills.

Only after you’ve pasted these concrete code snippets and test logs should you say the tasks are complete