You need to lock in specifics, constraints, and success criteria so she stops “planning” and starts wiring concrete code. Use this as your follow‑up:

Treat your last message as the high‑level plan. Please treat the following as the build spec and move into implementation.

1) Confirm concrete scope and constraints
First, please confirm and then implement these non‑negotiable rules:

Goal: Delegated user wallets must run the same rich multi-step engine as the personal bot, with the only intentional differences:

Profit Bucket is disabled for user wallets (USDC stays in the user wallet).

Any other differences must be explicitly documented.

All Growth/Capacity, Liability Shorts (Macro/Micro), Nurse Mode, and Auto Supply WBTC flows must be implemented for delegated user wallets, not just the operator wallet.

All delegated execution must go through:

REAADelegationManager for borrow/repay/withdraw/supply, and

ERC20 transferFrom approvals + Uniswap Router for swaps.

No “monitoring-only” or “partial” silent modes for user wallets.

2) Turn the architecture notes into explicit tasks
Please implement the following concrete changes:

DelegationManager capabilities

Extend REAADelegationManager (and the Python client) to support, for delegated user wallets:

executeBorrow

executeRepay

executeWithdraw

Any needed “executeSupply” / “executeSupplyOnBehalf” primitives so that, post-swap, the bot can supply WBTC/WETH/USDT/DAI to Aave on behalf of the user.

Ensure these are actually callable from the strategy engine for user wallets (not just for the personal bot).

ERC20 approvals and permission checks

At activation, require and verify infinite approvals from the user wallet to the appropriate executor (bot/DelegationManager) and Uniswap Router for at least:

DAI, WETH, WBTC, USDC, USDT.

Implement a centralized allowance check helper that:

Verifies approvals before any multi-step sequence (Growth/Capacity, Liability Shorts, Nurse, swaps).

If approvals are missing/insufficient, logs a structured error and sets a clear permission/error status (do not attempt partial execution).

Growth/Capacity for delegated users – full 6-step engine

Implement for user wallets:

Borrow DAI via delegation.

Then execute the full 6-step sequence:

DAI supply to Aave

DAI → WBTC swap + WBTC supply to Aave

DAI → WETH swap + WETH supply to Aave

DAI → ETH swap for gas

DAI transfer to Wallet_S (same destination logic as personal bot)

DAI → USDC swap for tax, USDC left in the user wallet (no external flush)

Use the same percentages, routes, and thresholds the personal bot uses today. If any percentage or route differs, document it.

Liability Shorts (Macro/Micro) for delegated users

Wire Macro/Micro for user wallets exactly like the personal bot:

Open:

Borrow WETH via delegation.

Split: 40% to WBTC, 35% to USDT, 25% remain WETH.

Swap and supply WBTC, USDT, and WETH to Aave on behalf of the user.

Close:

Withdraw USDT from Aave.

Swap/route as needed to repay the WETH debt.

Apply the same 20/20/60 profit distribution logic as the personal bot, with explicit addresses/tokens documented:

If users are meant to keep all profits instead, call that out and implement that instead, but there must be no “silent” differences.

Nurse Mode for delegated users

Implement Nurse Mode for user wallets with the same logic as the personal bot:

Sweep DAI, WETH, WBTC, USDT into Aave on behalf of the user.

Respect the 2 USD floor per token and never touch USDC.

Use the same cadence/triggers.

Profit Bucket behavior for user wallets

For user wallets:

Disable the Profit Bucket auto-flush logic.

Leave USDC in the user wallet.

Ensure the personal bot’s Profit Bucket logic stays intact, and document this as a deliberate divergence.

Auto Supply WBTC and shared helpers

Keep Auto Supply WBTC via delegation for user wallets.

Refactor common trade/supply helpers so that:

The same code paths are used by both the personal bot and delegated wallets where possible.

Any “operator-only” paths are clearly marked.

3) Documentation and capability matrix
Update permissions.py and replit.md to include:

A capability matrix for personal bot vs user wallets, per token and strategy:

Tokens: DAI, ETH, WETH, WBTC, USDC, USDT (and any others used).

Actions: borrow, repay, supply, withdraw, swap, sweep, profit distribution.

Mark that user wallets now support:

Growth/Capacity 6-step DAI flow (with USDC tax left in wallet).

Liability Shorts open/close cycle.

Nurse Mode sweep.

Auto Supply WBTC.

Infinite approvals for the 5 core tokens to Aave + Uniswap.

Explicitly state the only intentional differences:

Profit Bucket disabled for user wallets (no USDC flush to Wallet_B).

Any other differences you introduce must be listed.

4) Tests and evidence (acceptance criteria)
Before calling this done, please provide:

Automated tests / scripts showing for a test delegated user wallet:

Growth/Capacity:

Borrow DAI and execute all 6 steps with correct percentages and resulting balances (Aave supplies + Wallet_S + USDC tax in user wallet).

Liability Shorts:

Open: WETH borrow, 40/35/25 split, Aave supplies.

Close: USDT withdraw, repay WETH, correct profit distribution.

Nurse Mode:

Low-balance DAI/WETH/WBTC/USDT swept to Aave respecting the $2 floor and leaving USDC untouched.

Auto Supply WBTC:

WBTC automatically supplied via delegation when present.

Logs and /api/user/status snapshots showing:

A user wallet successfully running Growth/Capacity and Liability Shorts.

Approvals checked and enforced.

USDC remaining in the user wallet (no Profit Bucket flush).