Use this prompt to keep them thorough and prevent shortcuts. It restates what they’ve done, but adds explicit internal checkpoints, code expectations, and “no placeholder” rules.

Prompt to Replit: Deep Implementation Checklist (No Placeholder Architecture)

Thanks for the detailed audit; it’s very helpful. I want to lock in a thorough, production‑grade implementation of the REAA Command Center, with no placeholder flows or skipped steps.

Please treat the following as a checklist you must satisfy. For each section, confirm completion and, where indicated, show the relevant code snippet or path.

1. Auth & Token Flow – Full Pathway Verification
You already added get_current_user_id() and X-Auth-Token. Now:

Token creation

Confirm POST /api/auth/wallet:

Uses itsdangerous.TimestampSigner(APP_SECRET_KEY) to sign the user id.

Returns JSON like:

json
{
  "userId": 4,
  "walletAddress": "0x742d...",
  "towns": [...],
  "authToken": "4.aZaCSQ.XYBo..."
}
Include the actual function signature and the exact place where the token is created and added to the response.

Token consumption (no userId in client)

Confirm that all user‑scoped endpoints call get_current_user_id() and do not read userId from body or query:

/api/user/towns (GET/POST)

/api/defi/state

/api/income and /api/income/summary

/api/chat

Any others that need user scoping.

Internal checkpoint: run a quick grep to ensure there is no remaining userId = request.json.get( or args.get("userId").

Frontend API wrapper

Show the api() wrapper from consumer_dashboard.html that:

Adds X-Auth-Token when state.authToken is set.

Calls fullDisconnect() on any 401 before returning an error object.

Internal checkpoint: every call site (api('/api/...')) must go through this wrapper (no bare fetch calls for user APIs).

2. Wallet → REAA Command Center – State Sequence (No Placeholders)
I want a strict sequence for wallet state transitions. Please verify and, if needed, adjust the code so it behaves exactly like this:

Initial page load (disconnected)

state.authToken, state.userId, state.walletAddress are null.

Wallet button = “Connect Wallet” and bound to connectWallet().

DeFi panel shows -- and a clear message like “Connect wallet to view DeFi data.”

Health ring score = 0; ring appears in neutral color.

Pipeline: can show public town counts via /api/towns (OK to be non‑user‑specific).

Chat: shows initial REAA greeting without referencing a specific wallet.

After successful wallet connect

auth_wallet() is called, token and user record created.

Frontend:

Saves authToken, userId, walletAddress into state.

Updates wallet button text to truncated address and removes connectWallet handler.

Immediately calls:

loadDefiPanel()

loadIncomeSummary()

Any town/user calls needed (/api/user/towns if applicable).

Background thread:

Calls fetch_aave_position_for_wallet(walletAddress) and then upsert_defi_position.

This must be a real on‑chain call, not a stub. Confirm the web3/Aave client is configured and working.

DeFi panel:

Shows either real numbers (once thread completes) or a temporary “Loading DeFi data…” state.

No hard‑coded example values.

On any 401

fullDisconnect() clears all state variables (authToken, userId, walletAddress, chatHistory, chatBusy, defiPosition, safetyScore, userTownIds, towns).

Resets:

Wallet button → “Connect Wallet” with connectWallet() handler.

DeFi panel values to -- and “No recent actions.”

Health ring = 0.

Income panel to default 0/– values.

Pipeline → “No towns available” placeholder.

Chat window → initial REAA greeting.

Internal checkpoint: verify this visually in the browser (not only via automated tests).

If any of the above use placeholder text (e.g., fake numbers) or skip a step, please correct them and note where.

3. DeFi Integration – Concrete Implementation, Not Stub
You’ve added fetch_aave_position_for_wallet(). I want to ensure it is a real implementation:

Helper details

Provide the function signature and a brief pseudocode of what it does (e.g., which Aave contract / method, which RPC URL, how ETH → USD is calculated).

Confirm that if Aave returns zeros (no position), the helper returns None instead of a dummy object.

Upsert behavior

Show the database.upsert_defi_position(...) call (table fields).

Confirm you are not inserting any hard‑coded values (381.10 etc.).

GET /api/defi/state behavior

For positions found: JSON shape and example.

For no position: confirm it returns:

json
{
  "position": null,
  "message": "No Aave position detected for this wallet yet."
}
Internal checkpoint: test with one wallet that has an Aave position and one that doesn’t, and verify the UI states.

4. REAA Chat – Prompt, Context & Limits (No “Generic” Mode)
REAA must always run with rich context and safe limits.

System prompt & context

You already pasted the system prompt; please verify that on each /api/chat call, you actually build those context strings from:

get_user_towns() + get_filing_stats()

get_recent_filings_for_towns()

get_defi_position()

get_income_summary() + get_income_events()

Internal checkpoint: there should be no branch where /api/chat is called with an empty system prompt or without at least town/DeFi/filings context.

History handling

Confirm the frontend sends state.chatHistory.slice(-5) and the backend uses that in perplexity_chat_multi(system_prompt, history, user_message) when history is non‑empty.

Clarify the exact structure of history (e.g., {role, content}) and document it in replit.md.

Rate limiting

Confirm check_chat_rate_limit(user_id) is called at the top of /api/chat.

Provide the exact response JSON and HTTP status when the rate limit is exceeded.

Internal checkpoint: manually trigger the rate limit (more than 20 calls/min from one user) and verify:

API returns 429 with your error.

UI shows a friendly “please wait” message, not a generic failure.

5. Safety Score – No Divergence Between Client & Server
Right now, the 0–4 score is computed client‑side, and a label (“Excellent/Good/…”) is computed server‑side. To avoid future drift:

Document in replit.md the JS formula you’re using verbatim (already done, but keep it in a “Source of Truth” section).

Add a short comment in chat_api that safety labels are derived from health_factor directly, not from a separate score, so we know this is intentional and not a bug.

Internal checkpoint for later: if we ever need safety score server‑side (alerts), we will refactor the formula into a shared Python function.

No change needed now, just clear documentation so this doesn’t become accidental placeholder logic.

6. Wallet & Legacy Separation – Explicit Checks
You’ve said / and /app are independent; I want explicit checks:

Grep results for WALLET_S, WALLET_B, and the bot wallet address in:

templates/consumer_dashboard.html

The /api functions used by /app.

Confirm there are no imports or global variables shared from the admin dashboard into the /app template (e.g., no WorkingAgent state leaking into consumer views).

If any shared globals exist, please list them and confirm they don’t expose consumer data.

7. Internal QA Checklist (do not skip)
Before you consider this phase complete, please run this manual QA sequence and check off each step:

Visit /app → see disconnected REAA Command Center.

Connect a wallet with no Aave position:

See “No Aave position detected…” in DeFi panel.

Ask REAA a question → answer references towns/filings but not DeFi numbers.

Connect a wallet with real Aave data:

After a few seconds, see non‑zero health factor and safety ring update.

Ask REAA → answer references DeFi health and towns.

Open chat via avatar AND via “Ask REAA” button.

Trigger a 401 (e.g., clear authToken in localStorage and fire a request):

Confirm fullDisconnect runs and UI is back to clean “Connect Wallet” state.

Hit /api/chat >20 times in a minute:

Confirm rate‑limit message appears in UI.

If any of these fail or rely on placeholder data, adjust the code and mention what changed.