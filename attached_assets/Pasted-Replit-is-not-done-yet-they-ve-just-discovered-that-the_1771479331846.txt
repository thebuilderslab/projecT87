Replit is not done yet; they’ve just discovered that the Delegation Manager interactions are still stubs, and the architect is now pushing them to replace those with real Web3 calls. Right now Phase 2 is half‑way: DB + API + UI + bot plumbing exist, but the actual on‑chain supply is not wired.

You want everything fully functional, no mocks. Here’s an instruction prompt you can send them that spells out exactly what needs to be implemented and how, including code expectations.

Reply to Replit: Replace Delegation Stubs with Real Web3 Logic (No Mock Paths)

Thanks for the progress and for catching the remaining issues. At this point I want Phase 2 to be fully functional, with real on‑chain WBTC auto‑supply, not stubbed paths.

Please treat this as the definitive spec to complete Phase 2:

1. Delegation Manager – real Web3 integration
The delegation_client.py stubs need to be replaced with actual Web3 calls against a live Delegation Manager contract.

Environment & configuration

Use env vars:

ARBITRUM_RPC_URL – existing RPC.

DELEGATION_MANAGER_ADDRESS – deployed Delegation Manager contract address (I will provide / update this).

BOT_PRIVATE_KEY – bot signer (already used by the admin wallet).

Add a small ABI JSON for the Delegation Manager with at least:

text
function approveWBTCDelegation(uint256 maxAmount) external;
function revokeWBTCDelegation() external;
function autoSupplyWBTC(address user, uint256 amount) external;
In delegation_client.py, initialize:

python
w3 = Web3(Web3.HTTPProvider(ARBITRUM_RPC_URL))
delegation = w3.eth.contract(address=DELEGATION_MANAGER_ADDRESS, abi=DELEGATION_MANAGER_ABI)
bot_account = w3.eth.account.from_key(BOT_PRIVATE_KEY)
Functions to implement (no stubs)

In delegation_client.py, implement:

python
def is_contract_deployed() -> bool:
    # return False if DELEGATION_MANAGER_ADDRESS is empty OR code at that address is 0x
python
def build_approve_tx(user_address: str, max_amount_raw: int) -> dict:
    # used by frontend later if you decide to build tx client-side
python
def call_auto_supply_wbtc(user_address: str, amount_raw: int) -> str | None:
    """
    Called from auto_supply_wbtc_for_wallet().
    Returns tx_hash (hex string) on success, or None on failure.
    """
    # 1) Build transaction: delegation.functions.autoSupplyWBTC(user_address, amount_raw).build_transaction(...)
    # 2) Sign with bot_account
    # 3) Send raw transaction
    # 4) Wait for receipt with reasonable timeout
    # 5) If status == 1, return receipt.transactionHash.hex(), else None
    ```
No path should silently “pretend” success. If transaction fails or contract not deployed, return None and let the caller handle it.

2. auto_supply_wbtc_for_wallet – full, safe on‑chain execution
The function in auto_supply.py must perform the entire safety chain and only update DB on confirmed tx.

Safety chain (order is important)

In auto_supply_wbtc_for_wallet(user_id, wallet_address):

bot_enabled check: skip if database.is_bot_enabled(user_id) is False.

Managed wallet / delegation check:

Must be a row in managed_wallets.

delegation_status == "active" and auto_supply_wbtc == True.

Once‑per‑day check:

If last_auto_supply_at is within the last 24 hours, skip.

Contract deployed:

If is_contract_deployed() is False, log a wallet_actions record of type auto_supply_skipped_contract and return without DB updates.

On‑chain WBTC balance & allowance:

Read balance_raw = wbtc.balanceOf(wallet_address) (8‑decimals).

Read allowance_raw = wbtc.allowance(wallet_address, DELEGATION_MANAGER_ADDRESS).

Compute amount_raw with integer math only:

python
amount_raw = (balance_raw * 80) // 100  # 80% floor
amount_raw = min(amount_raw, allowance_raw)
If amount_raw <= 0 → skip with wallet_actions log auto_supply_skipped_amount.

Enforce amount_raw >= MIN_WBTC_SUPPLY_RAW (e.g., 0.01 WBTC in satoshis).

Transaction & DB updates

Call tx_hash = call_auto_supply_wbtc(wallet_address, amount_raw).

If tx_hash is None:

Record wallet_actions with action_type="auto_supply_failed" and details about balance, allowance, and attempted amount.

Do not update managed_wallets.supplied_wbtc_amount or last_auto_supply_at.

If tx_hash returned:

Convert to WBTC units for logging:

python
wbtc_decimals = 8
amount_wbtc = amount_raw / (10 ** wbtc_decimals)
Record wallet_actions with action_type="auto_supply_wbtc" and json details:

{"amount_raw": int(amount_raw), "amount_wbtc": float(amount_wbtc)} and tx_hash.

Update managed_wallets:

Increment supplied_wbtc_amount by amount_wbtc.

Set last_auto_supply_at = now().

This ensures DB updates are strictly gated on tx success.

3. API & UI behavior – no mock states
API

/api/delegation/activate and /api/delegation/revoke must:

Use get_current_user_id() only (no client-supplied userId).

Upsert managed_wallets with correct delegation_status and auto_supply_wbtc.

Log a wallet_actions record for “delegation_granted” / “delegation_revoked”.

Frontend

The Command Center must show real values from managed_wallets and wallet_actions:

Delegation status: none / active / revoked.

Auto‑supply WBTC: ON/OFF.

Last auto‑supply amount and timestamp (from wallet_actions).

When the Delegation Manager contract is not deployed (or is_contract_deployed returns False), UI should:

Disable the “Enable auto‑supply WBTC” control.

Display a clear “Coming soon / contract not deployed” message (no fake success state).

There should be no code path where the UI shows “Delegation active / auto‑supply working” if no contract address is configured or on‑chain calls are failing.

4. Testing checklist (no placeholders)
Before marking Phase 2 complete, please:

Unit‑style tests (you can do these via Python scripts or curl):

Case A: contract not deployed (DELEGATION_MANAGER_ADDRESS empty)

Confirm auto_supply_wbtc_for_wallet exits at the contract‑check guard, creates a wallet_actions record with type auto_supply_skipped_contract, and does not change supplied_wbtc_amount.

Case B: contract deployed, but balance/allowance zero

Confirm it logs auto_supply_skipped_amount and does not change DB.

Case C: normal case

Use a test wallet with WBTC balance + allowance sufficient for auto‑supply.

Confirm:

amount_raw equals floor(balance_raw * 0.8) and ≤ allowance.

A transaction is sent, receipt status 1.

wallet_actions has an auto_supply_wbtc entry with correct amount and tx_hash.

managed_wallets.supplied_wbtc_amount and last_auto_supply_at updated.

End‑to‑end UI tests in /app:

Wallet with no delegation → only read‑only view, delegation section shows “Not enabled”.

After activation → delegation status changes, auto‑supply toggle shows ON, and after the bot loop runs:

Command Center shows last auto‑supply amount/timestamp.

Revoke → status goes to revoked, and bot no longer calls auto_supply_wbtc_for_wallet for that wallet.

