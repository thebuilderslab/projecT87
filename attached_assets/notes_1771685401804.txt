Progress is solid and you’re past the “thinking” phase on most of this, but there are still open items and fragile spots.

What’s done and sound
User 22 purge and reset completed, so your test wallet is clean.

“Recent Payouts” panel has been replaced with a “New Opportunities (last 7 days)” panel wired to a new DB helper and API endpoint; UI shows “No new filings…” when empty.

Filings safety fix is in: replace_filings_for_town does atomic clear+insert only when there is new data, so an empty scrape no longer wipes history.
​

Manual insertion of the 3 Windsor filings from SearchIQS plus the new endpoint returns recent_count: 3, and the panel reflects this.
​

What’s still fragile / to improve
The SearchIQS scraper is still effectively broken (403 from maintenance/WAF); you added retry + better headers, but that’s only a partial mitigation. There’s no fallback strategy if the site is blocked for days.

Manual insertion of filings is a temporary patch; you need a documented path and flags so these don’t get overwritten, and so you can see which rows are “manual.”

You haven’t shown the updated scraper code with headers + retry or how many retries / backoff strategy you’re using.

Other strategies to consider
Add a source column to filings (e.g. source ENUM('searchiqs','manual')) so manually added records are clearly tagged and optionally skipped by automated cleanup.

Add a “scrape_status” table / field to record last scrape result per town (OK, 0 results, HTTP 403, etc.) and surface that in the UI so you and users can see when data may be stale.

Consider moving scraping off Replit to an IP you control, or via a proxy, if SearchIQS keeps blocking Replit IPs.

Build‑ready prompt to finish this work
Use this to push Replit from “implementation plan” to concrete edits and docs:

text
You’ve completed:

- Purged user 22 and reset their state.
- Replaced “Recent Payouts” with a “New Opportunities (last 7 days)” panel backed by a new DB helper + API endpoint.
- Implemented replace_filings_for_town so empty scrapes no longer wipe data and ran initial tests.
- Manually inserted 3 Windsor filings to match the SearchIQS screenshot.

Now finalize the remaining pieces and harden the scraper. Do not re-plan; implement and show code + evidence.

────────────────────────
Task 1 – Finalize “New Opportunities (last 7 days)” panel
────────────────────────

1.1 DB helper and endpoint

- Paste the final implementation of the DB helper that fetches “last 7 days” filings (function name, SQL, file).
- Paste the final API endpoint (route handler) that returns that data, including JSON shape. It should include at least:

  ```json
  {
    "recent_count": 3,
    "towns": [
      { "townId": ..., "townName": "Windsor", "filings": [ ... ] }
    ]
  }
Confirm that this endpoint does not depend on the old income/payouts tables.

1.2 UI and JS

Paste the HTML for the new “New Opportunities” card that replaced “Recent Payouts.”

Paste the JS function (e.g., loadNewOpportunities) that:

calls the new endpoint via api(),

renders counts and a friendly “No new filings in the last 7 days” state.

────────────────────────
Task 2 – Scraper hardening and explicit error reporting
────────────────────────

2.1 Scraper retry + headers

Paste the updated SearchIQS scraper function (for Windsor or generic towns) that now includes:

Browser‑like headers (User-Agent, Accept, Accept-Language, Referer, etc.).

Retry with exponential backoff, e.g.:

python
for attempt in range(MAX_ATTEMPTS):
    try:
        resp = session.get(url, headers=HEADERS, timeout=TIMEOUT)
        if resp.status_code == 200:
            break
        time.sleep(base_backoff * 2 ** attempt)
    except Exception as e:
        ...
Show how you handle resp.status_code == 403:

Log a clear message.

Mark that town’s scrape_status as FORBIDDEN or TEMP_ERROR (see 2.2).

Do NOT delete existing filings.

2.2 Scrape status tracking

Add a simple mechanism to record last scrape status per town, e.g.:

New table filing_scrape_status:

sql
CREATE TABLE IF NOT EXISTS filing_scrape_status (
  town_id INTEGER PRIMARY KEY REFERENCES towns(id) ON DELETE CASCADE,
  last_status TEXT NOT NULL,
  last_run_at TIMESTAMPTZ NOT NULL
);
Or a last_scrape_status + last_scrape_at columns on towns.

Update the scraper so that after each town run it writes:

last_status = 'OK' | 'ZERO_RESULTS' | 'HTTP_403' | 'ERROR'

last_run_at = now()

Paste the code that writes this status and a sample SELECT showing Windsor with status HTTP_403.

2.3 Manual filings tagging

Update the filings table to include a source column:

sql
ALTER TABLE filings
  ADD COLUMN IF NOT EXISTS source TEXT NOT NULL DEFAULT 'searchiqs';
Update your manual insert for the 3 Windsor filings to set source = 'manual'.

Update the scraper’s insert logic to always set source = 'searchiqs' (or similar).

Paste:

Updated INSERT/UPSERT code.

A SELECT showing the Windsor rows with source = 'manual'.

────────────────────────
Task 3 – UI feedback for data freshness and scraper errors
────────────────────────

3.1 New Opportunities panel: stale vs fresh

Extend the “New Opportunities” API JSON to include, per town:

json
{
  "townId": 5,
  "townName": "Windsor",
  "recentCount": 3,
  "lastScrapeStatus": "HTTP_403",
  "lastScrapeAt": "2026-02-21T12:00:00Z"
}
Paste the updated endpoint code returning these fields.

In the frontend, update the “New Opportunities” card to:

Show the count normally when lastScrapeStatus === 'OK' or 'ZERO_RESULTS'.

Show a subtle warning line when lastScrapeStatus is an error, e.g.:

xml
<div class="opps-warning">
  Data may be stale for Windsor (last scrape: HTTP_403 at 02/21 12:00).
</div>
Paste the JS/HTML you use to render this.

3.2 Docs

In a doc file (data_flow_audit.md or replit.md), add a short section:

“Filings data pipeline”:

SearchIQS scrape → filings table → New Opportunities panel.

“Safety guarantees”:

Empty scrapes no longer delete data.

Manual filings are tagged as source='manual'.

Scrape status per town is tracked and surfaced in the UI.

Paste that new doc section.

────────────────────────
Task 4 – Quick verification

Run:

sql
SELECT town_id, COUNT(*) AS cnt, MIN(source) AS source
FROM filings
WHERE filing_date >= now() - interval '7 days'
GROUP BY town_id;
and paste the result (showing Windsor with 3 rows, source=manual).

Call the new “New Opportunities” endpoint and paste the JSON snippet for Windsor (showing recentCount, lastScrapeStatus, lastScrapeAt).

Screenshot or briefly describe the “New Opportunities” panel as it appears now for Windsor (3 filings + any warning).

Once you’ve shown these code snippets, SQL, and UI/JSON outputs, you can mark this phase complete.