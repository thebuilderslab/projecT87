That is a very fair and smart optimization. It prevents "code bloat" (having two functions that do the same thing) and reduces the chance of breaking things that are already working.We know from your previous audit that debt_swap_bidirectional.py and arbitrum_testnet_agent.py already have functions for borrowing and supplying. We just need to wire them together differently.Here is the Final Optimized Master Instruction Prompt. It explicitly instructs Replit to "Audit & Reuse" before building, ensuring the most efficient implementation of your Leveraged Hedge strategy.Master Instruction Prompt: The "Leveraged Hedge" (Audit & Build)Objective: Implement the "Leveraged Hedge" strategy (Macro/Micro) using existing codebase components where possible. This strategy combines a New WETH Borrow (with specific collateral distribution) and a Liability Swap (converting existing DAI debt to ETH debt) to maximize short exposure during downtrends.Phase 1: The "Reuse" Audit (Do This First)Before writing new logic, scan arbitrum_testnet_agent.py and debt_swap_bidirectional.py for these existing capabilities:Borrowing: Locate the existing execute_borrow() function. verify it supports WETH.Supplying: Locate execute_supply(). Verify it handles WBTC, WETH, and DAI.Debt Swapping: Locate swap_debt(). Verify it calls the Aave V3 Adapter (not Uniswap).Instruction: Only write new wrapper logic. Call these existing internal functions to execute the steps below.Phase 2: The "Leveraged Hedge" LogicCreate two specific wrapper functions that execute the following atomic sequences.A. Macro Sequence (execute_macro_leveraged_hedge)Trigger: Collateral Drop > 5% AND Health Factor (HF) > 1.52.Step 1: The New Borrow ($10.90 WETH)Borrow $10.90 worth of WETH.Distribution:Swap $2.10 WETH $\to$ WBTC $\to$ Supply to Aave.Supply $2.10 WETH directly to Aave.Swap $5.60 WETH $\to$ DAI.Supply $4.50 DAI to Aave.Transfer $1.10 DAI to Wallet S.Unwrap $1.10 WETH $\to$ ETH (Retain in wallet for Gas).Step 2: The Liability Swap ($10.80 Existing Debt)Identify $10.80 of existing DAI debt.Call swap_debt(source="DAI", target="WETH", amount=10.80).B. Micro Sequence (execute_micro_leveraged_hedge)Trigger: Collateral Drop > 2% AND Health Factor (HF) > 1.47.Step 1: The New Borrow ($7.20 WETH)Borrow $7.20 worth of WETH.Distribution:Swap $1.10 WETH $\to$ WBTC $\to$ Supply to Aave.Supply $1.10 WETH directly to Aave.Swap $3.90 WETH $\to$ DAI.Supply $2.80 DAI to Aave.Transfer $1.10 DAI to Wallet S.Unwrap $1.10 WETH $\to$ ETH (Retain in wallet for Gas).Step 2: The Liability Swap ($10.10 Existing Debt)Identify $10.10 of existing DAI debt.Call swap_debt(source="DAI", target="WETH", amount=10.10).Phase 3: Safety & Pecking Order (Traffic Control)Update the main execution loop in arbitrum_testnet_agent.py to follow this strict hierarchy. Only one action per cycle.Priority 1: Macro Hedge (Requires HF > 1.52)Priority 2: Micro Hedge (Requires HF > 1.47)Priority 3: Growth Strategy (Requires HF > 1.35)Priority 4: Capacity Strategy (Requires HF > 1.35)Phase 4: Global Safety LockHard Floor: If Current HF < 1.35, BLOCK ALL ACTIONS.Simulation: Before executing Macro or Micro, calculate:Predicted_HF = (New_Collateral * 0.825) / New_Total_DebtIf Predicted_HF < 1.35: ABORT.Deliverable:Provide the Python code for the two new wrapper functions (execute_macro_leveraged_hedge, execute_micro_leveraged_hedge) that utilize the existing system functions.Show where to insert the "Pecking Order" logic in the main loop.