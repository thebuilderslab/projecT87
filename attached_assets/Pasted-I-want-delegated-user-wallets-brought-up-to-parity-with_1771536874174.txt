I want delegated user wallets brought up to parity with the personal bot, with these exact behaviors and exceptions:

1) Rich multi-step engine for user wallets
Please implement the same multi-step execution engine for user wallets that the personal bot uses, with routes and percentage distributions copied over, except where I specify changes below.

For Growth/Capacity, user wallets should now do:

Borrow DAI → distribute across 6 steps:

DAI supply to Aave

DAI → WBTC swap + WBTC supply

DAI → WETH swap + WETH supply

DAI → ETH swap for gas

DAI transfer to Wallet_S

DAI → USDC swap for tax, left in the user’s wallet (not sent out)

Any remaining dust or tokens not handled by these steps should be picked up by Nurse Mode (see below).

2) Liability Shorts for user wallets (Macro/Micro)
Wire Macro/Micro Liability Shorts for user wallets exactly like the personal bot:

Borrow WETH → split:

40% to WBTC

35% to USDT

25% stays as WETH

Supply WBTC, USDT, and WETH to Aave.

On close:

Withdraw USDT → swap as needed → repay WETH debt.

Apply the same 20/20/60 profit distribution you use for the personal bot (document which addresses/tokens each slice goes to, and make sure user wallets follow that same logic or clearly document if they receive all profits themselves).

If any part of this flow is currently hard-coded to the operator wallet, please refactor so it works for delegated user wallets as well, with the same routes and percentages.

3) Nurse Mode for user wallets
Enable Nurse Mode for user wallets:

Sweep DAI, WETH, WBTC, and USDT into Aave for the user,

Respect the same 2 USD floor and “never touch USDC” rule the personal bot uses,

Use the same thresholds and scheduling logic.

4) Changes to Profit Bucket behavior
For user wallets only, skip the Profit Bucket auto-flush:

Do not auto-flush USDC to Wallet_B at 22 USDC.

Leave USDC in the user’s wallet instead.

The personal bot may keep using the Profit Bucket if you want, but user wallets should retain their own USDC.

5) Auto Supply WBTC and Force Approve
Keep and standardize:

Auto Supply WBTC for user wallets via the delegation manager.

Force Approve: user wallets must grant infinite approvals for the same 5 tokens (list them explicitly) to:

Aave Pool

Uniswap (or the configured DEX router)

Also:

Add robust ERC20 allowance checks so that if approvals are missing or revoked, the system surfaces a clear permission error instead of silently degrading behavior, and guides the user to re-approve.

6) Implementation and docs expectations
Please:

Update the strategy engine so that user wallets follow the same multi-step sequences and percentages as the personal bot for:

Growth/Capacity (6-step DAI distribution as above)

Liability Shorts (WETH borrow, 40/35/25 split, supply, close, and profit distribution)

Nurse Mode (sweep behavior)

Auto Supply WBTC

Ensure all required swaps and supplies for user wallets are wired through Uniswap/Aave with proper approvals and error handling.

Update permissions.py and replit.md to reflect:

That user wallets now run the full multi-step engine, with:

Growth/Capacity 6-step DAI distribution (with the USDC tax left in-wallet).

Liability Shorts flow.

Nurse Mode.

Auto Supply WBTC.

The only intentional differences from the personal bot:

Profit Bucket is disabled for user wallets (USDC stays in wallet).

Any other differences you must explicitly call out.

Provide test evidence (logs + /api/user/status) showing a user wallet:

Running the full Growth/Capacity 6-step sequence.

Successfully executing a Liability Short open and close with the correct splits and profit distribution.

Nurse Mode sweeping dust assets as designed.

Keeping USDC in-wallet (no flush to Wallet_B).

Having the required infinite approvals set and verified.

This is the target: user wallets get the same rich multi-step automation as the personal bot, with routes and percentages copied over, Profit Bucket disabled, and USDC tax left in the user’s wallet.