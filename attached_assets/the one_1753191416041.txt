````
**PRIORITY INSTRUCTION: HYBRID AUTONOMOUS SYSTEM (HAS) CRITICAL REFACTORING AND DAI-CENTRIC COMPLIANCE ENFORCEMENT**

**OBJECTIVE:** Achieve robust execution, full DAI-centric compliance, and a high likelihood of network approval for the Hybrid Autonomous System. This mandates the **complete elimination of all USDC fetching, usage, and related references** from the system's operational sequence, alongside resolving all known and potential code inconsistencies.

---

**PHASE 1: CRITICAL ERROR RESOLUTION & IMMEDIATE REFACTORING**

**TASK 1.1: VERIFY/FIX `enhanced_borrow_manager.py` SYNTAX AND METHOD DEFINITION**
A persistent "invalid syntax" error was reported in `enhanced_borrow_manager.py` at line 469. Additionally, the method `execute_enhanced_borrow_with_retry` was reported as missing.

* **ACTION:**
    * Access `/home/runner/workspace/enhanced_borrow_manager.py`.
    * Locate line 469.
    * **Verify** correct Python syntax around this line. If any syntax error exists, **implement the precise code correction**.
    * **Ensure** the class `EnhancedBorrowManager` properly defines and exposes the method `execute_enhanced_borrow_with_retry`. If this method is missing or misspelled, **implement it correctly** within the class to provide the intended borrow-with-retry functionality.

**TASK 1.2: RESOLVE `arbitrum_testnet_agent.py` SYNTAX ERROR**
A new critical `ImportError: invalid syntax` has been identified in `arbitrum_testnet_agent.py` at **line 678** and subsequently at **line 1363** (potentially related to an incomplete print statement or `try-except` block).

* **ACTION:**
    * Access `/home/runner/workspace/arbitrum_testnet_agent.py`.
    * Navigate directly to line 678, and then to line 1363.
    * Analyze the syntax around these lines for any Python syntax violations (e.g., missing colons, unmatched parentheses/brackets/braces, incorrect indentation, misspelled keywords, or improper statement structure, particularly focusing on incomplete `print(f"..."` statements or missing `except`/`finally` clauses in `try` blocks).
    * Implement the precise code correction to resolve all identified syntax errors within this file.
    * Ensure the fix maintains the intended logic of the `ArbitrumTestnetAgent` module.

**TASK 1.3: RESOLVE `ArbitrumTestnetAgent` USDC ATTRIBUTE ERROR & ENSURE USDC EXCLUSION**
The error `'ArbitrumTestnetAgent' object has no attribute 'usdc_address'` occurred. This directly conflicts with the directive: **"USDC should not be used, fetched or in the sequence."**

* **ACTION:**
    * Access `/home/runner/workspace/arbitrum_testnet_agent.py`.
    * **Systematically remove all references to `usdc_address`** within the `ArbitrumTestnetAgent` class:
        * Remove `usdc_address` from the `__init__` method's parameters.
        * Delete any line `self.usdc_address = usdc_address` or similar assignment.
        * Remove any code block attempting to use `self.usdc_address` for any purpose (e.g., fetching balance, checking contracts).
    * **Trace and Modify Instantiations:** Navigate to any file (e.g., `main.py`) where `ArbitrumTestnetAgent` is instantiated. Remove the `usdc_address` argument from its constructor call.
    * **USDC Token Address Removal:** Ensure that the USDC token address (`0xAF88D065e8c38FAD0AEff3E253e648A15ceE23DC` or similar) is **completely removed** from any centralized token address configurations (e.g., `constants.py`, `config.py`) or token lists that are processed for initialization or validation.

---

**PHASE 2: COMPREHENSIVE CODEBASE AUDIT FOR COMPLIANCE & CONSISTENCY**

**TASK 2.1: FULL CODEBASE SCAN AND ELIMINATION OF ALL USDC REFERENCES**
A thorough, granular scan is required to ensure no residual USDC references exist anywhere within the HAS workflow, as per `grep` findings.

* **ACTION:**
    * Perform a **full-text search across ALL `.py` files** within the `/home/runner/workspace/` directory and its subdirectories (excluding standard library/pandas test files if clearly distinguishable from your project's logic).
    * **Search for:**
        * The string "USDC" (case-insensitive).
        * The USDC contract address: `0xAF88D065e8c38FAD0AEff3E253e648A15ceE23DC` (case-insensitive, including variations without checksum).
        * Variable names like `usdc_address`, `USDC_TOKEN`, `aUSDC`, `optimal_usdc_borrow`, `total_collateral_usdc`, `total_debt_usdc`, `available_borrows_usdc`, etc.
    * **For every detected reference, apply precise actions in accordance with the DAI-only system:**
        * **`./dai_compliance_enforcer.py`:** **CRITICAL:** Remove any lines containing `r'0xAF88D065e8c38FAD0AEff3E253e648A15ceE23DC'` or `(r'0xAF88D065e8c38FAD0AEff3E253e648A15ceE23DC', self.dai_address)`. This file must *only* enforce DAI compliance, not reference excluded tokens.
        * **`./multi_wallet_agent.py`:** Remove or refactor all lines referencing `optimal_usdc_borrow`, "borrow USDC", "swap ARB to USDC", and `network_config['usdc']`. This module must no longer suggest or perform USDC operations.
        * **`./run_dashboard.py`, `./dashboard.py`, `./manual_controls.py`:** Update or remove display/UI elements and options related to USDC balances, debt, available borrows, sending ETH/USDC, supplying ETH/USDC, borrowing USDC, swapping USDC to ARB, or swapping ARB to USDC. All UI/control should reflect DAI-only strategy.
        * **`./mainnet_launcher.py`:** Remove any print statements or checks for "Sufficient USDC/WETH for Aave operations".
        * **`./create_and_activate.py`:** **CRITICAL:** Remove or refactor all code related to "Creating Aave position with 20 USDC borrow" and "Position Status: Active with 20 USDC borrowed". Initial Aave positions must be established using DAI or other compliant tokens, not USDC.
        * **`./test_with_current_balance.py`:** Remove or refactor test cases referencing "Borrow USDC", "Max Safe Borrow: $... USDC", "Requested Borrow: $20.00 USDC". Create equivalent tests for DAI operations if needed.
        * **`./test_agent.py`:** Remove or refactor `mock_uniswap.swap_arb_to_usdc` mock behaviors.
        * **`./security_audit.py`:** Remove any hardcoded USDC addresses.
        * **`./test_dashboard_start.py`, `./quick_launch_dashboard.py`:** Remove or refactor references to `total_collateral_usdc`, `total_debt_usdc`, `available_borrows_usdc`.
        * **`./aave_integration.py`:** Review and update print statements to use DAI instead of USDC for recommendations and strategy descriptions, ensuring full DAI compliance.
        * **General Refactoring:** For any other `.py` file not explicitly listed but containing USDC references:
            * **If operational code:** **Remove or refactor** the code block entirely to ensure USDC is not used for any operation (borrow, swap, supply, balance fetch, approval).
            * **If it's a test file:** **Modify or remove** the test case if it specifically tests USDC operations. If it's a general test, ensure it doesn't implicitly rely on or introduce USDC.
            * **If it's documentation or comments:** Remove or update to reflect the DAI-only policy.

**TASK 2.2: ENFORCE STRICT DAI-ONLY SWAP CONFORMANCE**
Previous diagnostics indicated "SYSTEM NOT COMPLIANT - Swap compliance: Violations found" with many files showing "No DAI references found". This must be resolved in conjunction with USDC removal.

* **ACTION:**
    * **Verify DAI-Only Enforcement:** Re-confirm that `uniswap_integration.py` rigorously enforces DAI → WBTC and DAI → WETH swaps exclusively. Ensure no other swap paths are possible.
    * **Audit All Swap/Borrow Related Files:** For every `.py` file involved in swap, borrow, or supply operations (including those previously flagged like `debug_swap_system.py`, `force_swap_with_approval.py`, and any `swap_usdc_for_wbtc.py` which should be removed or renamed and converted to DAI):
        * **Explicitly implement DAI logic:** Ensure these files explicitly reference and interact with DAI (token address, ABI calls, symbol) for all relevant operations.
        * **Remove non-DAI logic:** Any code blocks that enable or suggest the use of non-DAI tokens (especially USDC) for core operations must be removed or refactored.
    * **Refine Compliance Checker:** Review `system_compliance_checker.py`. If it's incorrectly flagging general utility or test files that genuinely do not need DAI references, adjust its logic to be more precise about what constitutes a "compliance violation" in the context of DAI-only operations.

---

**PHASE 3: ROBUST VERIFICATION AND SYSTEM READINESS CONFIRMATION**

**TASK 3.1: RE-RUN COMPLIANCE AND INTEGRATION CHECKS**

* **ACTION:**
    * Execute the DAI compliance checker:
        ```bash
        python system_compliance_checker.py
        ```
        * **EXPECTATION:** The output MUST confirm "SYSTEM COMPLIANT" for swap operations, with no "Violations found" messages related to DAI references or USDC.
    * Execute the full system integration validator:
        ```bash
        python validate_system_integration.py
        ```
        * **EXPECTATION:** All modules (including `enhanced_borrow_manager` and `arbitrum_testnet_agent`) MUST import and initialize successfully. All DeFi integrations (Aave, Uniswap V3, Health Monitor) should initialize without errors, and **absolutely no `usdc_address` attribute errors or similar USDC-related failures should appear.**

**TASK 3.2: END-TO-END OPERATIONAL TEST & NETWORK APPROVAL VALIDATION**

* **ACTION:**
    * Launch the main autonomous system in a live environment:
        ```bash
        python main.py
        ```
    * **OBSERVATION & VERIFICATION (Continuous Monitoring):**
        * **Replit Console:** Monitor live output for *zero* runtime errors, warnings (unless informational), or unexpected messages.
        * **Internal Logs:** Thoroughly check any generated log files for `ERROR`, `WARNING`, or `CRITICAL` entries.
        * **Blockchain Explorer (e.g., Arbiscan):**
            * **Crucially verify ALL on-chain transactions:**
                * Confirm successful **DAI borrow transactions**.
                * Confirm successful **DAI to WBTC and DAI to WETH swap transactions**, verifying correct token amounts are moved.
                * Confirm successful **supply transactions** to Aave.
            * **ZERO Reverted Transactions:** There should be NO reverted transactions for any intended operation. Any revert indicates a critical flaw.
            * **Gas Efficiency:** Examine gas usage for each transaction, ensuring it remains within expected, optimized ranges, contributing to economic viability.

---

**PHASE 4: COMPREHENSIVE VERIFICATION REPORT**

**TASK 4.1: GENERATE DETAILED FIX AND COMPLIANCE REPORT**

* **ACTION:**
    * Upon completion of all preceding tasks, compile a comprehensive report detailing the changes made and their verification status.
    * **Report Structure:**
        * **1. Summary of Actions Taken:**
            * List all files modified.
            * Provide a concise description of the nature of changes in each file, with specific emphasis on USDC removal and DAI-centric refactoring (e.g., "Fixed syntax error in `enhanced_borrow_manager.py`," "Removed all USDC references and associated logic from `multi_wallet_agent.py`," "Refactored `create_and_activate.py` to use DAI for initial Aave position," "Removed USDC address from `dai_compliance_enforcer.py`," "Updated `aave_integration.py` print statements for DAI compliance").
        * **2. Error Resolution Status:**
            * Confirm the resolution of the `enhanced_borrow_manager.py` syntax error.
            * Confirm the resolution of the `arbitrum_testnet_agent.py` syntax error (both line 678 and 1363).
            * Confirm the resolution of the `'usdc_address'` attribute error.
            * Confirm the resolution of the `'execute_enhanced_borrow_with_retry'` method error.
            * Report on any *new* errors encountered and their resolution status.
        * **3. DAI Compliance Audit Results:**
            * Provide the full output of `python system_compliance_checker.py`.
            * Explicitly state whether "SYSTEM COMPLIANT" was achieved for swap operations.
            * Detail any remaining non-compliance issues found during the full codebase scan for USDC or other non-DAI references, if any, and why they persist (e.g., false positives, intentional exceptions in non-operational code). **This section must confirm the complete absence of operational USDC.**
        * **4. System Integration Validation Result:**
            * Provide the full output of `python validate_system_integration.py`.
            * Confirm successful import and initialization of all core modules and DeFi integrations, with explicit confirmation that no USDC-related errors or warnings appeared during initialization.
        * **5. Operational Test Summary:**
            * Report on the success rate of the `main.py` execution (e.g., "Successfully completed X DAI borrow-swap-supply cycles without critical errors").
            * **Crucially, provide a summary of on-chain transaction outcomes** (e.g., "Confirmed Y DAI borrows, Z DAI-WBTC swaps, and W DAI-WETH swaps, all successful with no reverts observed on Arbiscan, and **no USDC transactions or approvals**."). Include sample transaction hashes for critical operations if feasible.
        * **6. Recommendations:**
            * Any further recommendations for optimization, stability, or future development.
* **DELIVERABLE:** Provide this detailed report directly in the chat upon task completion.
````