TASK: Extract all files used in current debt swap sequence execution, generate code summaries, and analyze for replication of successful onchain Aave debt swap logic.

## OBJECTIVE:
1. List all files/scripts referenced in the debt swap workflow
2. For each file: generate concise summary of role and core logic
3. Cross-reference successful wallet execution logs (system logs + Arbitrum onchain transaction logs)
4. Replicate proven onchain logic and call structure from successful logs
5. Cross-check with other successful Aave debt swap executions by different wallets on Arbitrum

## CRITICAL NEXT-STEP INSTRUCTIONS:

### 1. Use Official ParaSwap Documentation and ABI for multiSwap (V5):
- Reference official ParaSwap V5 GitHub repo: https://github.com/paraswap/augustus-v5
- Use Augustus Swapper V5 ABI on Etherscan: https://etherscan.io/address/0xdef171fe48cf0115b1d80b88dc8eab59176fee57#code
- Extract parameter layouts, struct definitions, and selector reference
- Map struct: SellData → Path → Adapter → Route

### 2. Ensure Proper Adapter Address Resolution:
- System currently lacks real onchain adapter contract addresses for Arbitrum
- multiSwap struct REQUIRES adapter addresses (placeholders will cause failure)
- Search ParaSwap main repo (NOT forks) and Arbitrum deployment metadata for adapter addresses
- Security critical: ONLY use official ParaSwap sources for adapter mappings

### 3. Consider Direct UniswapV3 Approach (Production Shortcut):
- If adapter addresses unavailable or resolution too slow, switch to Direct UniswapV3 method (Augustus V6.2)
- Simpler: no adapter mapping required
- Proven compatible with Debt Switch and mainnet for "exact out" swaps
- Selector: 0x5e94e28d (swapExactAmountOutOnUniswapV3)

### 4. Simulation and Confirmation Before Mainnet:
- Perform eth_call with constructed calldata (regardless of method)
- Ensure simulation passes selector and structure validation
- ONLY proceed to mainnet if simulation matches expected outcome and structure

### 5. Keep Logging and Audit Rigor High:
- Log every call, attempted adapter address, and struct item
- Enable fast debugging and traceability
- Document all parameter mappings and route constructions

## DELIVERABLES:
1. Complete file list with paths and purposes
2. Code summaries for each file (role, core logic, dependencies)
3. Analysis of successful transaction logs from both:
   - Your wallet's system logs
   - Arbitrum onchain transaction logs (via Arbiscan)
4. Comparison with other successful Aave Debt Switch transactions on Arbitrum
5. Recommendations for replicating proven successful call structure

## OUTPUT FORMAT:
```
FILE INVENTORY:
- [file path]: [summary of role and logic]

LOG ANALYSIS:
- Successful transactions from your wallet
- Cross-reference with public Aave debt swap executions
- Common patterns and call structures

RECOMMENDATIONS:
- Path forward (V5 multiSwap vs V6.2 Direct UniswapV3)
- Required parameter mappings
- Simulation strategy before mainnet
```

Await further instructions to tune/replicate code according to proven success logs.