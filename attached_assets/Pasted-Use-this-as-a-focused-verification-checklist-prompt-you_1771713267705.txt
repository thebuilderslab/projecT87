Use this as a focused verification/checklist prompt you can paste into your assistant:

You are reviewing an implementation of nonce-based CSP for a FastAPI + Starlette WSGIMiddleware + Flask stack. Verify the following, step by step, and report any failures with concrete evidence (headers, HTML snippets, or logs):

Per‑request nonce consistency (single request)

For a single HTML request, capture the HTTP response headers and the rendered HTML.

Extract the CSP header value and the nonce inside script-src 'nonce-…'.

Extract all <script> tags and their nonce attributes.

Confirm that:

Every inline <script> that should execute has a nonce attribute.

Every such nonce value is identical to the nonce in the CSP header for that same response.

There are no inline <script> tags without a nonce.

If any mismatch occurs, show the exact header and <script> snippets.

Cross‑layer nonce propagation (FastAPI → WSGI bridge → Flask)

Instrument or log the value of the nonce at three points for the same request:

When the FastAPI middleware generates it.

When the custom WSGI wrapper injects it into the WSGI environ.

When Flask reads it (context processor) and passes it to the template as csp_nonce.

Verify that all three logged values are identical for a single request and differ across different requests.

Verify this for:

Developer portal page.

REAA dashboard page.

Admin dashboard page.

Report any route where the values diverge or where the nonce is missing at one of the three points.

Coverage of all HTML responses

Enumerate all routes that return HTML (including error handlers, redirects that end in HTML, login pages, admin dashboard, partial views, and any legacy templates).

For each HTML route:

Confirm that the CSP header with a nonce is present.

Confirm that any inline <script> tags in that response have the correct matching nonce.

Identify any HTML route that:

Lacks a CSP header, or

Contains inline scripts without a nonce, or

Uses a different nonce than the header.

CSP policy adequacy for actual resources

Capture a representative browsing session (network and console logs) through all main flows of the app (login, dashboards, DeFi operations).

From the network log, list all external domains actually used for:

Scripts.

XHR/fetch/websocket connections.

Stylesheets and fonts.

Images, if relevant.

Compare this list against the current CSP directives. Confirm that:

Arbitrum RPC endpoints needed by the app are allowed under the correct directive (typically connect-src).

Google Fonts are correctly covered (style-src for fonts.googleapis.com, font-src for fonts.gstatic.com).
​

WalletConnect endpoints are allowed under connect-src/frame-src/img-src as needed.
​

Any other third‑party services (analytics, Sentry, CDNs, etc.) actually used by the app are explicitly allowed.

Report any console CSP violation errors and which directive needs adjustment.

Inline handlers and dynamic script behavior

Search the entire codebase (templates and generated HTML) for:

Inline event handlers (onclick=, onload=, etc.).

javascript: URLs.

Dynamic script creation or eval/new Function usage.

Confirm that:

Either such patterns are eliminated, or

The CSP policy and implementation strategy account for them without re‑introducing unsafe-inline or unsafe-eval except where strictly necessary.

If you must use unsafe-eval or similar, document which libraries require it and why, and assess the impact on nonce effectiveness.

Security properties and invariants

State explicitly the intended security property in one sentence (for example: “No attacker-controlled inline script can execute unless it is injected by our server and tagged with the per‑request nonce”).

For each of the checks above, explain how the results support or violate this property.

Conclude whether, given the evidence, the current implementation satisfies the intended property, and list any remaining gaps or TODOs.

Use real runtime data (captured responses and logs), not just code inspection, and present the final result as a concise pass/fail summary per item 1–6, with concrete examples where something fails.