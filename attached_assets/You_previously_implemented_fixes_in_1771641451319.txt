You previously implemented fixes in this repo for DelegationManager + strategy engine, including executeBorrow/executeWithdraw forwarding, delegatedwithdraw, nonce management, and nurse sweep.

New information and constraints
- Your last iteration introduced an architectural shortcut:
  - You modified delegatedborrow and the Growth distribution to keep borrowed DAI in the BOT wallet and removed pulltokenfromuser from steps 3, 4, 6, and 7.
  - This change bypasses the user wallet and centralizes user funds in the bot wallet.
- That shortcut is NOT acceptable. The intended design is:
  - Borrow flows: DM → User wallet.
  - Swaps and distributions: BOT wallet pulls from User wallet via ERC20 allowance.
  - Nurse and other sweeps may use DM where DM already has token approvals.
- We now assume the FRONTEND will handle the missing approvals:
  - At wallet connection, the user signs ERC20 approve transactions giving:
    - DelegationManager contract a max allowance for its required tokens (already in place).
    - BOT wallet a max allowance for the same key tokens (DAI, USDC, WBTC, WETH, and any other assets used in Macro/Micro/Growth/Capacity distributions).
- DM is intentionally narrow:
  - It should only handle Aave primitives (executeSupply, executeBorrow, executeRepay, executeWithdraw, emergencyWithdrawToken), not arbitrary swaps.
  - Swaps are executed off-chain by the BOT wallet using on-chain DEXs (1inch/Uniswap/etc.).

Your tasks now:
You are moving from “implementation plan ready” to “build ready.” Implement concrete code changes and documentation that restore the intended architecture, harden the “joints,” and make Nurse and strategy flows safe under the clarified approval model.

Do this in the following order and show code + reasoning at each step.

────────────────────────
Task 1 – Revert architectural shortcut and restore user‑wallet centric flow
────────────────────────

1.1 Revert delegatedborrow and related helpers
- In delegationclient.py (and any related module), revert the behavior where:
  - borrowed DAI stops in the BOT wallet, and
  - distribution steps are changed to operate only from the BOT wallet balance.
- Restore the original pattern:
  - DM executeBorrow is called.
  - Aave sends borrowed tokens to DM (msg.sender).
  - DM forwards the borrowed tokens to the USER wallet (either atomically in the contract or via the existing 3‑step pattern: borrow → emergencyWithdrawToken → transfer_to_user).
- Confirm that:
  - _forward_borrowed_tokens_to_user (or equivalent helper) is ONLY used for moving tokens out of DM, not for swaps.

1.2 Restore pulltokenfromuser in ALL distributions
- For Macro, Micro, Growth, Capacity distributions, restore pulltokenfromuser at every step where the BOT wallet needs to operate on user funds:
  - Step 2: DAI supply.
  - Steps 3–4: DAI→WBTC/WETH swaps and supply.
  - Steps 5–7: gas reserve, WalletS, USDC tax, or any similar steps.
- Ensure each of these steps uses the pattern:
  1) pulltokenfromuser(user, token, amount)  // ERC20.transferFrom(user → BOT)
  2) BOT executes swap / supply / transfer.
- Do NOT introduce new “keep everything in bot wallet” shortcuts.

1.3 Integrate the new approval model
- Assume that, when the user connects their wallet, the frontend has already executed approve(tx) for:
  - user → DelegationManager: max allowance for required tokens.
  - user → BOT wallet: max allowance for the same tokens.
- In code and comments:
  - Remove any workaround that assumes the BOT wallet has zero allowance.
  - Add explicit precondition comments where pulltokenfromuser is used, e.g.:

    // PRECONDITION: user has granted BOT wallet ERC20 allowance for this token.

- Add a short “approvals.md” (or update replit_wallet_audit.md) describing:
  - Which allowances exist.
  - Which components depend on each allowance (DM vs BOT).
  - The fact that front-end signing at connection is responsible for granting BOT-wallet approvals.

Show:
- The updated delegatedborrow code.
- One representative distribution function (e.g., Growth 7‑step distribution) with restored pulltokenfromuser calls and comments.

────────────────────────
Task 2 – System‑wide wallet & approvals audit (Macro/Micro/Growth/Capacity + Nurse)
────────────────────────

2.1 Enumerate approvals and flows
- Search the codebase for:
  - any ERC20.approve or transferFrom usage,
  - pulltokenfromuser,
  - any DM executeBorrow/executeSupply/executeRepay/executeWithdraw calls,
  - emergencyWithdrawToken,
  - nurse/sweep/safety functions.

- Produce a markdown section in replit_wallet_audit.md that for each strategy family (Macro, Micro, Growth, Capacity, Nurse) lists:

  - Which tokens are involved.
  - Where those tokens reside at each step (user wallet, BOT wallet, DM, Aave pool).
  - Which approvals are required (user→DM, user→BOT, BOT→Aave pool, DM→Aave pool).

2.2 Check “joints” for fragility
For each of the following, show code snippets and notes:

- Any place that assumes:
  - tokens are in user wallet when they might be in bot or DM, or
  - aTokens belong to DM when they actually belong to user.
- Any remaining call to pulltokenfromuser in Nurse/sweep or other flows that would fail without BOT wallet approval.
- Any “rescue” function (e.g., emergencyWithdrawToken) that may leave dust or break accounting.

For each fragile joint:
- Explain the issue.
- Implement a concrete fix if possible without redeploying contracts.
- If contract changes are required, add a clearly labeled TODO in the solidity and replit_wallet_audit.md.

────────────────────────
Task 3 – Personal bot vs Delegation path comparison (concrete, in‑repo)
────────────────────────

Create or update replit_wallet_audit.md with a concise table for each action type:

Columns:
- Action type (borrow, supply, swap, repay, withdraw, nurse).
- “Personal bot” path:
  - which address initiates the tx,
  - whose allowance is used,
  - where tokens end up.
- “Delegation” path:
  - DM method (if any),
  - onBehalfOf / msg.sender details,
  - where tokens end up.
- Differences / risks.

Specifically highlight:
- That we rely on:
  - USER→DM approvals for executeBorrow/executeRepay/executeSupply/executeWithdraw flows that pull from user.
  - USER→BOT approvals for pulltokenfromuser used by swaps and wallet‑level distributions.
- Confirm 
