# Inside your strategy_manager.py (or similar file)
# Ensure MIN_ETH_GAS_THRESHOLD is imported if defined in config.py
# e.g., from config import MIN_ETH_GAS_THRESHOLD
import traceback # Ensure this is imported for detailed error reporting

def execute_leveraged_supply_strategy(self, agent, amount_to_borrow_dai=10):
    print("\n🚀 Starting REVISED DAI-BASED LEVERAGED SUPPLY STRATEGY...")

    # --- Phase 1: Borrow DAI (10 DAI) ---
    print(f"Step 1: Attempting to borrow {amount_to_borrow_dai} DAI...")
    try:
        # Adapt 'borrow_asset' to your agent's actual method and parameters
        success_borrow = agent.borrow_asset(agent.dai_address, amount_to_borrow_dai)
        if not success_borrow:
            print("❌ Failed to borrow DAI. Aborting strategy.")
            return False
        print(f"✅ Borrowed {amount_to_borrow_dai} DAI successfully.")
    except Exception as e:
        print(f"❌ Error during DAI borrow: {e}")
        traceback.print_exc() # Print full traceback
        return False

    # --- Phase 2: Conditional ETH for Gas (1 DAI allocation) ---
    # This decision point makes your gas management dynamic.
    print("\nStep 2: Managing ETH for gas (conditional)...")
    try:
        current_eth_balance = agent.get_wallet_eth_balance() # Adapt this method call
        print(f"  Current Wallet ETH: {current_eth_balance:.6f} ETH (Threshold: {MIN_ETH_GAS_THRESHOLD} ETH)")

        if current_eth_balance < MIN_ETH_GAS_THRESHOLD:
            print(f"  Wallet ETH is below threshold. Swapping 1 DAI for ETH.")
            # Adapt 'swap_token' for DAI to ETH
            success_eth_swap = agent.swap_token(agent.dai_address, agent.eth_address, 1) # Swap 1 DAI for ETH
            if not success_eth_swap:
                print("  ⚠️ Failed to swap 1 DAI for ETH. This might impact future transactions if gas runs out.")
        else:
            print(f"  Wallet ETH is sufficient. Supplying 1 DAI to Aave as collateral instead of swapping for ETH.")
            # Adapt 'supply_asset' for DAI
            success_supply_eth_dai_yield = agent.supply_asset(agent.dai_address, 1) # Supply the 1 DAI as DAI
            if not success_supply_eth_dai_yield:
                print("  ⚠️ Failed to supply 1 DAI (intended for ETH) to Aave. This portion is not supplied.")
    except Exception as e:
        print(f"❌ Error during ETH gas management: {e}")
        traceback.print_exc()
        # Decide if to return False or continue with a warning. For gas, often continue is fine.

    # --- Phase 3: Core Collateral Building (WBTC, WETH, DAI) ---
    print("\nStep 3: Building core collateral in Aave (7 DAI equivalent)...")

    # Swap 3 DAI for WBTC and supply
    try:
        print("  Swapping 3 DAI for WBTC and supplying to Aave...")
        # If you have a single `swap_and_supply` method, use it. Otherwise, chain `swap_token` and `supply_asset`.
        success_wbtc_supply_swap = agent.swap_and_supply(agent.dai_address, agent.wbtc_address, 3) # Adapt this method
        if not success_wbtc_supply_swap:
            print("  ❌ Failed to swap 3 DAI for WBTC and supply. This portion is not in collateral.")
            # Decide if to abort here or continue with partial success
    except Exception as e:
        print(f"❌ Error during WBTC supply swap: {e}")
        traceback.print_exc()

    # Swap 2 DAI for WETH and supply
    try:
        print("  Swapping 2 DAI for WETH and supplying to Aave...")
        success_weth_supply_swap = agent.swap_and_supply(agent.dai_address, agent.weth_address, 2) # Adapt this method
        if not success_weth_supply_swap:
            print("  ❌ Failed to swap 2 DAI for WETH and supply. This portion is not in collateral.")
    except Exception as e:
        print(f"❌ Error during WETH supply swap: {e}")
        traceback.print_exc()

    # Supply 2 DAI directly
    try:
        print("  Supplying 2 DAI directly to Aave...")
        success_dai_supply = agent.supply_asset(agent.dai_address, 2) # Adapt this method
        if not success_dai_supply:
            print("  ❌ Failed to supply 2 DAI directly. This portion is not in collateral.")
    except Exception as e:
        print(f"❌ Error during direct DAI supply: {e}")
        traceback.print_exc()

    # --- Phase 4: WBTC to Wallet (2 DAI allocation) ---
    print("\nStep 4: Swapping 2 DAI for WBTC to leave in wallet...")
    try:
        # Adapt 'swap_token' for DAI to WBTC for wallet
        success_wbtc_wallet_swap = agent.swap_token(agent.dai_address, agent.wbtc_address, 2) # Swap 2 DAI for WBTC
        if not success_wbtc_wallet_swap:
            print("  ⚠️ Failed to swap 2 DAI for WBTC for wallet. This portion will not be in wallet.")
        else:
            print("  ✅ 2 DAI worth of WBTC successfully acquired and left in wallet.")
    except Exception as e:
        print(f"❌ Error during WBTC wallet swap: {e}")
        traceback.print_exc()

    print("\n🎉 REVISED DAI-BASED LEVERAGED SUPPLY STRATEGY SEQUENCE COMPLETED.")
    return True # Indicate overall success of the sequence