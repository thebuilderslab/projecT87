### Phase 1: Fix Core Blocking Errors & Data Integrity

**Task 1.1: Resolve Aave Integration Failures**
Analyze and correct the `arbitrum_testnet_agent.py` and `market_signal_strategy.py` files to resolve the `AttributeError: 'NoneType' object has no attribute 'eth'` and `'get_user_account_data'` errors. This likely indicates that the Aave client or provider is not being initialized correctly before being used. Implement a robust connection check at the start of the `_execute_market_signal_operation()` method to ensure the Aave protocol client is properly instantiated and connected to the Arbitrum Testnet.

**Task 1.2: Implement API Rate Limit Handling**
Modify the `enhanced_market_analyzer.py` file to handle `429 Client Error: Too Many Requests` from CoinAPI and CoinMarketCap. Implement a defensive strategy using **exponential backoff with jitter** for all API calls. The `EnhancedMarketAnalyzer` class should retry failed requests with a randomized delay, logging the rate-limiting error without crashing.

### Phase 2: On-Chain Execution & Smart Contract Integration

**Task 2.1: Correct Uniswap V3 Integration**
In `uniswap_integration.py`, resolve the "Missing ARB Swap Implementation" error. The Uniswap integration is for a debt swap, which is a key misunderstanding in the existing code. Uniswap is for token swaps, not debt swaps. A true debt swap uses a smart contract adapter.

**Task 2.2: Implement the Debt Swap Adapter**
Create a new function, `execute_debt_swap_adapter()`, within the `arbitrum_testnet_agent.py` file. This function will be the sole method for executing the debt swap. It must not use Uniswap directly for the debt swap itself. Instead, the function must use a web3 library (e.g., `web3.py` or `ethers.py`) to call a pre-audited third-party debt swap adapter contract on Arbitrum. The function needs to:
1.  Connect to the Arbitrum Testnet.
2.  Use the `_get_aave_client()` method to get the user's current debt position.
3.  Call the `swapDebt()` function on a reliable Aave debt swap adapter contract (e.g., from BGD Labs or ParaSwap).
4.  Pass the necessary parameters: the user's address, the amount of DAI debt to repay, the DAI token address, the ARB token address, and the ARB token contract details for the new debt position.

**Task 2.3: Integrate Correct Addresses and ABIs**
Find and use the correct smart contract addresses and ABIs for the following on the Arbitrum **testnet**:
- The DAI token contract.
- The ARB token contract.
- The Aave V3 Pool contract.
- The Aave V3 Debt Swap Adapter contract.
Hardcode these addresses into the `arbitrum_testnet_agent.py` or a separate configuration file to resolve the "missing ARB swap implementation" and other on-chain execution failures.

### Phase 3: Final Logic Integration & Comprehensive Validation

**Task 3.1: Revise Execution Flow Logic**
Revise the `arbitrum_testnet_agent.py` to reflect the new, correct logic:
1. The `_execute_market_signal_operation()` should first call `validate_debt_swap_readiness()` and then `check_arb_depreciation_for_debt_reduction()` which uses the `enhanced_market_analyzer` and `TechnicalAnalysis` classes.
2. It should then call the new `execute_debt_swap_adapter()` function to perform the on-chain debt swap transaction.
3. Remove any code that attempts to use Uniswap directly for a "debt swap." Uniswap should only be used if the system is ever expanded to perform general token swaps.

**Task 3.2: Enhance Pre-Execution Validation**
In `arbitrum_testnet_agent.py`, update `validate_debt_swap_readiness()` to include the following checks before any transaction is broadcast:
- Aave health factor must be `> 2.0`.
- The wallet must have enough ETH to pay for transaction fees.
- The `debt_swap_active` flag must be set to `True`.

**Task 3.3: Debug & Test**
Add print statements and logging throughout the final script to clearly show the status of each step: API calls, indicator calculations, trading signal generation, health factor checks, and the final on-chain transaction broadcast. This will allow for proper debugging on Replit and verification of a successful execution.