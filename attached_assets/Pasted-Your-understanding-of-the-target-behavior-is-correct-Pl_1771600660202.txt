Your understanding of the target behavior is correct. Please treat the following as the final build spec and move from “plan” to implementation.

1. Final Liability Short close distribution for delegated user wallets
When a delegated user wallet closes a Liability Short and realizes profit 
P
P (in WETH after debt repayment):

Implement exactly this allocation (all percentages are of the total profit 
P
P):

20% → Wallet_S (DAI)

Swap 20% of 
P
P from WETH → DAI.

Transfer DAI to Wallet_S.

20% → USDC to user wallet

Swap 20% of 
P
P from WETH → USDC.

Transfer USDC to the user wallet.

30% → WBTC supplied to Aave

Swap 30% of 
P
P from WETH → WBTC.

Supply WBTC to Aave onBehalfOf the user.

20% → WETH supplied to Aave

Keep 20% of 
P
P as WETH.

Supply that WETH to Aave onBehalfOf the user.

10% → USDT supplied to Aave

Swap 10% of 
P
P from WETH → USDT.

Supply USDT to Aave onBehalfOf the user.

Any residual WETH remaining in the DelegationManager/bot after these slices (due to rounding, slippage, or partial failures):

Swap residual WETH → ETH.

Send ETH to the user wallet as gas/safety net.

The personal bot’s existing 20/20/60 logic remains unchanged.

2. Execution and failure model
Keep the per‑slice best‑effort model, but make it explicit and observable:

Each slice (Wallet_S, USDC, WBTC, WETH, USDT) is executed independently.

If one slice fails (e.g., USDC swap revert), the other slices still attempt to execute.

Any unprocessed WETH from a failed slice:

Contributes to the final residual WETH that is swapped to ETH and sent to the user wallet.

3. Logging and observability (required)
Implement structured logging for each close:

For each slice, log:

json
{
  "event": "short_close_slice",
  "wallet": "<user_wallet>",
  "slice": "Wallet_S | USDC | WBTC | WETH | USDT",
  "percent_of_profit": 20 | 20 | 30 | 20 | 10,
  "status": "OK" | "FAILED",
  "amount_in_weth": "<float>",
  "amount_out_token": "<float>",
  "reason": "<error if FAILED>"
}
For the residual sweep, log:

json
{
  "event": "short_close_residual_sweep",
  "wallet": "<user_wallet>",
  "token_in": "WETH",
  "amount_in_weth": "<float>",
  "amount_out_eth": "<float>",
  "status": "OK" | "FAILED",
  "reason": "<error if FAILED>"
}
If the residual sweep fails, leave the WETH in the user’s economic domain (user wallet or easily recoverable) and log the failure.

4. Code changes to implement now
Short close handler

Update the Liability Short close function for delegated wallets to:

Compute 
P
P in WETH after repay.

Derive the 20/20/30/20/10 splits as above.

Call the existing Uniswap/Aave helpers to implement each slice and the residual sweep.

Permissions / profiles

Update permissions.py:

Ensure the delegated user wallet profile reflects:

Profit slices: 20% Wallet_S (DAI), 20% USDC (wallet), 30% WBTC (supplied), 20% WETH (supplied), 10% USDT (supplied).

Note that only Wallet_S and USDC slices are direct wallet transfers; the others are Aave supplies.

Docs

Update replit.md Profit Flow Comparison so it clearly shows:

Personal bot (unchanged): current 20/20/60 breakdown.

Delegated user wallet (Liability Short close):

20% → Wallet_S (DAI)

20% → USDC to user wallet

30% → WBTC supplied to Aave

20% → WETH supplied to Aave

10% → USDT supplied to Aave

Residual WETH → ETH to user wallet as gas

Explicitly mention that distribution is per‑slice best effort, not atomic, and that Nurse Mode + future strategies will clean up any leftover tokens over time.

5. Acceptance criteria
Before marking this done, please provide:

A test run (e.g., profit P = 100 units) showing:

≈20 units worth to Wallet_S (DAI).

≈20 units worth as USDC in user wallet.

≈30 units worth as WBTC supplied.

≈20 units worth as WETH supplied.

≈10 units worth as USDT supplied.

Logs demonstrating:

All five slices logged with status: "OK" in the happy path.

A simulated failure for one slice (e.g., USDC) with:

That slice logged as FAILED.

Other slices OK.

Residual WETH sweep logged.

Please proceed to implement this distribution and open a PR with code, logs, and doc updates.