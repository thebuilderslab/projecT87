3. Swap Execution & Safety (CRITICAL FIXES)
When performing swaps in Growth/Capacity, you must fix two critical omissions:
- Missing DEX Allowance: After pulling tokens to the BOT, the BOT must approve the DEX_ROUTER to spend the tokens BEFORE calling the swap function. Add an `approve_dex_router(token, amount)` helper and call it before every swap.
- Stuck Funds Rollback: Wrap the actual swap execution in a try/except block. If the swap transaction reverts (e.g., due to slippage or gas limits), the except block MUST transfer the pulled tokens back to the USER before aborting the step. 

Example required pattern:
```python
# 1. Pull
pull_success = pull_token_from_user(user, token_in, amount)
if not pull_success: return abort()

try:
    # 2. Approve Router
    approve_dex_router(token_in, amount)
    
    # 3. Swap
    swap_tx = execute_swap(...)
    
    # 4. Send output to User
    send_to_user(token_out, expected_out)
    
except Exception as e:
    log.error(f"Swap failed: {e}. Executing rollback...")
    # 5. ROLLBACK: Send original tokens back to user
    send_to_user(token_in, amount)
    return abort()

### Final Insight on `USE_ATOMIC_BORROW`

Your plan mentions keeping the `USE_ATOMIC_BORROW` feature flag and testing the `DM -> BOT -> USER` fallback. 

**My strong recommendation:** If you have already deployed the updated DelegationManager contract with the `executeBorrowAndTransfer` function we discussed previously, **delete the fallback code entirely.** Maintaining a feature flag for a broken, unsafe architectural path (holding user funds in the bot) will only cause headaches and edge-case bugs during testing. Force the system to use the atomic path; if it fails, fix the smart contract, don't patch the Python bot.
