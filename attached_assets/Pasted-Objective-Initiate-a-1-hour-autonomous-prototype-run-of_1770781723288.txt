Objective: Initiate a 1-hour autonomous prototype run of arbitrum_testnet_agent.py to verify the end-to-end $10.20 Growth and $5.50 Capacity paths.1. Comprehensive Supply Function Audit:Before any execution, the bot must perform a pre-flight audit of the Supply Logic:Logic Verification: Confirm the bot uses the Proportional Recovery scaling factor (current_dai / original_need) if a crash is detected, preventing "all-or-nothing" failures.Trigger Audit: Verify that the Growth Trigger ($12 cap) correctly falls through to the Capacity Trigger ($7 cap) if capacity is insufficient.Network Approval Efficiency: The bot must call allowance() for both the Aave Pool and Uniswap Router before every individual swap or supply step. If the allowance is < $15.00, it must execute an approve() transaction immediately.Execution Verification: Every supply() and transfer() call must be wrapped in a transaction confirmation listener. The bot cannot move to the next step until it receives a successful receipt hash from the Arbitrum network.2. Detailed Audit & Monitoring Strategy:If the bot encounters an error during the 1-hour run, it must execute the following Audit Protocol:Step 1 (The Nonce Sync): If a transaction fails with a "Nonce too low" or "Replacement transaction underpriced" error, the bot must call get_transaction_count(pending) and reset its local counter before retrying.Step 2 (The Dust Guard): If a swap fails with "Execution Reverted," the bot must check the trade value. If it is < $1.00, it must automatically convert that amount to a direct DAI Supply to Aave to protect the Health Factor instead of retrying the swap.Step 3 (Log Verbosity): Increase log detail for the _execute_fixed_distribution method. Every successful network approval, swap, and supply must be logged with its specific Arbiscan Transaction Hash.3. Success Parameters for the 1-Hour Run:Clean Start: Start with a deleted execution_state.json.Autonomous Monitoring: The bot must complete at least one 130s sleep cycle and one full Monitoring check successfully.State Wipe: The state file must only be deleted after the final $1.10 (or recalibrated remainder) DAI is confirmed at the WALLET_S_ADDRESS.Baseline Anchor: After a successful run, verify that last_collateral_value_usd is updated to the current market value (e.g., $66.75) to prevent loop-firing.Technical Breakdown of the Prototype RunFeatureAudit RequirementGoal for 1-Hour SuccessSupply LogicProportional scaling for leftovers.Clear all "dust" DAI from the wallet.TriggerFall-through from Growth to Capacity.Utilize current $9.72 capacity for a $5.50 move.Networkallowance() check per step.100% success rate for Aave/Uniswap interactions.PersistenceStep-by-step execution_state.json.Survive at least one simulated process restart.