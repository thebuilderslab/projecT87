Use this as your next prompt to the Replit agent:

“Your last breakdown looks good. For clarity and final documentation, please do the following:

Phase 1 – Growth/Capacity (DAI debt, 40/35/25 ratios)

Restate the full lifecycle for the DAI-driven paths, step by step, with explicit token flows and amounts:

Borrow: Confirm that in Growth and Capacity paths the bot borrows DAI from Aave as the only debt token.

Initial DAI distribution: Show exactly how DAI is split:

Portion swapped to WBTC and supplied as WBTC collateral.

Portion swapped to WETH and supplied as WETH collateral.

Portion converted to USDT and supplied as USDT collateral.

Gas reserve in native asset.

Portion converted/swapped as needed for Wallet S (ending as DAI) and Wallet B (ending as USDC), if applicable in this phase.

Ratios:

Growth Path: Apply and clearly label the 40/35/25 split (WBTC/WETH/USDT) on the $8.00 collateral block, with the exact numeric amounts.

Capacity Path: Apply the constraint that USDT supply is fixed at $1.10, and show how the remaining $2.20 is split between WBTC and WETH using the normalized 40/35 ratio. List all three final collateral amounts.

Collateral rule: Explicitly confirm that whenever DAI is being supplied to Aave (Nurse Mode, Capacity Path, resupply, recovery, etc.), it is always routed as DAI → WETH → USDT → supply to Aave, and no raw DAI is ever supplied.

Phase 2 – Macro/Micro Short Entry (WETH debt, example allocation)

I want the Macro/Micro short entry to follow the example allocation you proposed where borrowed WETH is split into multiple collateral types rather than 100% USDT. Use that structure and make it explicit:

Borrow: Confirm that for Macro and Micro shorts the bot borrows WETH from Aave and that WETH is the only debt token for these shorts.

Entry allocation (collateral mix): For each of Macro Short and Micro Short:

Start from the nominal WETH borrow notional you used ($10.90 for Macro, $7.20 for Micro).

Show the exact dollar allocations with the growth-style split you proposed (e.g., 40% WBTC, 35% WETH, 25% USDT for Macro; apply the same percentages to Micro).

For each slice, show:

WETH → WBTC swap and supply as WBTC collateral (for the WBTC slice).

WETH retained and supplied directly as WETH collateral (for the WETH slice).

WETH → USDT swap and supply as USDT collateral (for the USDT slice).

Hedge note: Briefly note how this diversified collateral mix changes the pure-hedge behavior compared to 100% USDT, but still implement it as requested.

Short Close – Profit Distribution (20/20/60, token endpoints)

Clearly document the liability short close flow using the already-implemented 20/20/60 logic, making token movements explicit:

Close:

USDT → WETH to repay the WETH debt.

Remaining USDT after debt repayment = remaining_usdt_balance.

Distribution:

20% Wallet S (Savings):

Compute as integer: wallet_s_amount = int(remaining_usdt_balance * 0.20).

Route: USDT → WETH → DAI.

Final action: Transfer DAI to WALLET_S.

20% Wallet B (Yield):

Compute as integer: wallet_b_amount = int(remaining_usdt_balance * 0.20).

Route: USDT → USDC (direct swap preferred, USDT → WETH → USDC as fallback).

Final action: Transfer USDC to WALLET_B.

60% Collateral (Growth):

Compute as integer: collateral_amount = remaining_usdt_balance - wallet_s_amount - wallet_b_amount.

Action: Supply this remaining USDT back to Aave as USDT collateral.

Integer math: Explicitly confirm that every percentage-based calculation is cast to int(...) before being used in any Web3 call, and that decimals (6 vs 18) are handled correctly.

System-wide summary – functions and distributions

Provide a clean, human-readable breakdown of all system functions and distributions in one place, organized as:

Growth Path (DAI debt → WBTC/WETH/USDT collateral, Wallet S/B flows, gas).

Capacity Path (DAI debt → WBTC/WETH/USDT with USDT fixed at $1.10, Wallet S/B flows, gas).

Macro Short (WETH debt → example allocation across WBTC/WETH/USDT collateral, then close and 20/20/60 distribution).

Micro Short (WETH debt → scaled-down version of the same allocation and close logic).

Nurse/Resupply/Recovery rules (DAI always routed to USDT before supply, USDT Collateral Architecture preserved).

Format this as a clear textual spec with bullets and, where helpful, concrete numeric examples (like the $10.20/$5.50/$10.90/$7.20 scenarios), so it can be used as the canonical description of how the system functions and how every dollar or token is ultimately distributed