Update and clarify the spec as follows, then audit the implementation against it:

Growth and Capacity phases – add USDC held in wallet

Please update the system description and logic so that:

Collateral supplied to Aave in Growth and Capacity is still only WBTC, WETH, and USDT (no USDC as collateral).

However, a designated percentage of the initial DAI working capital is swapped into USDC and left in the main wallet (Wallet B) as liquid USDC, not supplied to Aave.

This “USDC left in wallet” slice should be clearly separated from:

The WBTC/WETH/USDT collateral allocations, and

Any later USDC that arrives from the 20/20/60 profit split on liability short close.

After updating the spec, please show me numerically, for both Growth and Capacity paths, how a sample starting DAI amount is now split into:

Gas reserve

Wallet S (DAI)

Wallet B (USDC left in wallet)

WBTC collateral

WETH collateral

USDT collateral

USDC across the whole system

Explicitly enumerate all places where USDC exists in the system with the updated rules:

Growth/Capacity: USDC portion left in Wallet B (not supplied).

Profit distribution (short close): 20% of remaining USDT swapped to USDC and sent to Wallet B.

Confirm there are no other USDC usages (no USDC debt, no USDC collateral on Aave).

Sequence audit for the current task list

Using the updated spec, review the recent actions you took when executing these tasks:

Change money split for growth

Adjust money limits for capacity

Change how short trades are made

Update system rules and flows

Restart systems and check operations

For each task above:

Describe the actual sequence of token flows you implemented (DAI → WBTC/WETH/USDT/USDC, WETH → WBTC/USDT, etc.).

Confirm whether the new “USDC left in wallet” behavior is currently coded, or if it still needs to be added.

Call out any mismatches between:

The new spec (with USDC held in wallet in Growth/Capacity), and

The current code paths for Growth, Capacity, Macro Short, Micro Short, and the liability short close.

Fixes and next steps

If any behavior is not yet aligned with the updated spec:

List a short task list (Task 1, Task 2, …) with:

File name

Function(s) to update

A one-line summary of the change (for example, “peel off X% of initial DAI to USDC and leave in wallet before collateral routing”).

Implement those changes.

Re-run the pre-flight/audit summaries and print:

Growth path distribution breakdown (including USDC in wallet).

Capacity path distribution breakdown (including USDC in wallet).

Macro/Micro short entry breakdown.

Liability short close 20/20/60 breakdown, including where USDC goes.

Keep all prior constraints in place (USDT Collateral Architecture, 20/20/60 split, DAI→WETH→USDT routing for any DAI that is actually supplied), and do not expose Wallet S on the dashboard.