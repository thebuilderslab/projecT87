Objective: Complete the "Predictive Targets" upgrade, verify critical WETH permissions, and execute a "Live Plumbing Test" to prove the Liability Short logic works on-chain.Step 1: Finish Predictive Trigger LogicContext: You are currently upgrading the monitor to be predictive (calculating exact dollar triggers) rather than reactive.Action:Ensure arbitrum_testnet_agent.py calculates the exact Dollar Value for the triggers at the start of every cycle:micro_trigger_usd = last_collateral_value_usd * 0.98macro_trigger_usd = last_collateral_value_usd * 0.95Log Update: Modify the IDLE log output to explicitly show these targets.Example Output: "📉 Liability Short Targets: Micro < $98.00 | Macro < $95.00 (Current: $100.00)"Step 2: WETH Credit Delegation Check (Critical)Context: We verified the Adapter Address (0x63df...) but not the user's WETH permission. The Debt Swap will fail if this is missing.Action:Create and run a script named check_weth_delegation.py.Logic:Connect to the Aave V3 WETH Variable Debt Token contract.Call borrowAllowance(user_wallet, spender=0x63dfa7c09Dc2Ff4030d6B8Dc2ce6262BF898C8A4).Output:If allowance > 0: Print "✅ WETH Credit Delegation is ACTIVE."If allowance == 0: Print "❌ WETH Credit Delegation MISSING. Must approve before testing."Step 3: The "Plumbing Test" (Force Execution)Context: We cannot wait for a market crash to verify the complex "Borrow $\to$ Split $\to$ Swap" logic. We must force a tiny test run now.Action:Create a script named test_liability_plumbing.py.Logic:Initialize the Agent.Force Execute the Micro Distribution logic (Part A) with a hardcoded $2.00 WETH borrow amount.Bypass all Health Factor and Collateral Drop triggers for this test.Skip Part B (Liability Swap) for now—we just want to prove the "Borrow & Distribute" sequence works.Goal: Output the Transaction Hash for the WETH Borrow and the subsequent Swaps (WETH $\to$ WBTC/DAI).Run this script immediately after the delegation check passes.Execution Order:Commit the Predictive Code.Run check_weth_delegation.py.If (and only if) Delegation passes, Run test_liability_plumbing.py.Proceed.