You’ve already done the high‑level audit:

- Found that clear_filings_for_town() deletes all filings for a town before inserting new ones, which wiped 35 filings when a later scrape returned 0.
- Noted several defi_positions rows with wallet_address = NULL.
- Observed that updated_at is tracked but never surfaced in the UI.
- Pointed out the clear+insert gap where the UI can see an empty filings table during a scrape.

Now move from “implementation plan ready” to “build ready.” Implement the fixes with code and migrations, and show evidence. Do not re‑plan.

────────────────────────
Task 1 – Make filings updates safe and non‑destructive
────────────────────────

Goal: Avoid losing historical filings when a scrape returns 0 or fails, and avoid a window where the UI sees 0 rows.

1.1 Update clear_filings_for_town logic

- Paste the current implementation of clear_filings_for_town() in real_estate_tasks.py (or wherever it lives).
- Replace the pattern:

  - DELETE FROM filings WHERE town_id = %s
  - then insert new rows

  with a safer approach. You may choose one of:

  **Option A – “Delete only if we have new data” (minimum change)**

  - Fetch new filings for that town into a list `new_filings`.
  - If len(new_filings) == 0:
    - Log a warning and **do not** delete existing rows.
  - Else:
    - Start a DB transaction.
    - DELETE FROM filings WHERE town_id = %s.
    - INSERT new_filings using executemany/UPSERT.
    - COMMIT.

  **Option B – UPSERT by unique key (recommended)**

  - Define a natural key per filing (e.g. (town_id, docket_number, filing_date)).
  - Add a UNIQUE index on that combination in the DB (see Task 1.2).
  - Upsert individual filings with INSERT … ON CONFLICT DO UPDATE (no bulk delete).

- Implement the chosen option in code. Paste the updated function.

1.2 Add/adjust DB constraints for filings

- Show the current CREATE TABLE for filings.
- Write a migration (or alter script) that:

  - Adds a unique constraint if needed, e.g.:

    ```sql
    ALTER TABLE filings
      ADD CONSTRAINT filings_unique_town_docket
      UNIQUE (town_id, docket_number, filing_date);
    ```

  - Optionally adds an index on (town_id, filing_date DESC) to keep queries fast.

- Paste the migration SQL and the final CREATE TABLE definition.

1.3 Transaction-level atomicity

- Ensure that the new scraper flow (delete+insert or upsert) runs inside a single transaction per town.
- Show the Python code that opens the transaction (using your db helper / context manager) and commits/rolls back on error.
- Demonstrate with logs:

  - One scraper run where a town has N > 0 filings: show count before and after.
  - One scraper run where a town temporarily returns 0 filings: show that existing DB rows are **not** deleted.

────────────────────────
Task 2 – Clean up NULL wallet_address rows in defi_positions
────────────────────────

Goal: Ensure every defi_positions row has a valid wallet_address and that queries/UI don’t get confused by NULLs.

2.1 Inspect and migrate existing rows

- Run a SELECT showing the rows with wallet_address IS NULL:

  ```sql
  SELECT id, user_id, wallet_address, collateral_usd, debt_usd, health_factor
  FROM defi_positions
  WHERE wallet_address IS NULL;
Determine, from managed_wallets or users, what wallet_address should be for each row (if any).

Write a migration script that either:

Updates these rows with the correct wallet_address:

sql
UPDATE defi_positions dp
SET wallet_address = mw.wallet_address
FROM managed_wallets mw
WHERE dp.user_id = mw.user_id
  AND dp.wallet_address IS NULL;
OR, if they are truly stale/invalid, deletes them:

sql
DELETE FROM defi_positions
WHERE wallet_address IS NULL;
Paste the SQL and the “after” SELECT showing 0 rows with wallet_address IS NULL.

2.2 Enforce non-NULL going forward

Update the table definition so wallet_address is NOT NULL if that’s consistent with your logic:

sql
ALTER TABLE defi_positions
  ALTER COLUMN wallet_address SET NOT NULL;
(Only if safe after the migration.)

In the monitoring code (where you upsert defi_positions), show that you always pass a real wallet address; if there’s any path that could write NULL, fix it.

────────────────────────
Task 3 – Surface updated_at / staleness in the UI
────────────────────────

Goal: Users should see how fresh their DeFi data is.

3.1 Backend: expose last_updated in API

Identify the endpoint(s) used for the main DeFi/Command Center cards (e.g. GET /api/user/status or /api/defi/state).

Show the handler’s code and the SELECT from defi_positions.

Add a field in the JSON response, e.g.:

json
{
  "defi": {
    "collateralUsd": 140.53,
    "debtUsd": 39.14,
    "healthFactor": 2.83,
    "lastUpdated": "2026-02-21T11:42:35.850Z"
  }
}
Paste the updated handler code and an example JSON response.

3.2 Frontend: display “Last updated”

In the UI section that shows Collateral / Debt / Net Worth / HF (Command Center or DeFi tab):

Add a small text line like:

xml
<div class="defi-last-updated" id="defiLastUpdated">
  Last updated: --
</div>
Wire it in JS:

javascript
function renderDefiSummary(defi) {
  // existing HF/collateral/debt rendering…

  const el = document.getElementById('defiLastUpdated');
  if (defi.lastUpdated) {
    el.textContent = `Last updated: ${formatTimestamp(defi.lastUpdated)}`;
  } else {
    el.textContent = 'Last updated: --';
  }
}
Implement a formatTimestamp helper that formats to your existing style (e.g. 2026-02-21, 11:42 AM).

Paste the HTML + JS snippets.

────────────────────────
Task 4 – Document the data flow and new safety guarantees
────────────────────────

4.1 data_flow_audit.md (or update replit.md)

Add a short markdown section that:

Describes the current data flow for filings:

SearchIQS scrape → real_estate_tasks.py → filings table → /api/filings → Filings tab / Lead Pipeline.

States the new guarantees:

Scrapes that return 0 filings no longer wipe existing data.

Each town’s filings are updated atomically per run.

defi_positions rows always have a wallet_address.

DeFi cards show a “Last updated” timestamp.

Paste that new doc section.

4.2 Quick verification checklist

Show:

A successful scraper run log for a town with filings, with counts before/after.

A run where the scraper returns 0 result for a town and the DB retains previous rows.

Screenshot or JSON+UI snippet showing lastUpdated visible for a user.

────────────────────────
Constraints

Do NOT change the high‑level business logic (strategy engine, thresholds) unless required for the above fixes.

Prefer small, explicit DB migrations and code edits with clear before/after examples.

For each fix, show the relevant code/migration and at least one real query/log as evidence that it works.