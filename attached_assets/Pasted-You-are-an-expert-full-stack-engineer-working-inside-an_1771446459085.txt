You are an expert full‑stack engineer working inside an existing Replit project that already runs an Aave‑based “Real Estate Liquidity Hub / Command Center.”
Your tasks are:

Audit the existing codebase and wallet logic.

Refactor it so the consumer‑facing app uses the connected wallet (end user) instead of a hard‑coded wallet, while still preserving the existing internal wallets and distribution logic.

Add a clean initialization flow: when a new user connects, the app searches for stablecoins in their wallet, supplies them to Aave, and only then activates the safety/health meter and lead pipeline.

Prepare the project for the UI redesign described below (central agent + chat, towns selection, Replit SQL DB).

Follow the instructions step by step and output all requested artifacts.

A. Codebase audit (current Repl)
Scan all files and identify:

Where the hard‑coded wallet address is used (e.g., const wallet = "0x...").

Where Replit secrets WALLET_S and WALLET_B (or similar names) are referenced.

Any DeFi / Aave integration files (e.g., aave.ts, defi.ts, liquidityEngine.ts).

Any distribution or payout loops that send funds from or to those wallets.

Produce a concise report with:

A list of relevant files and functions, with file paths.

For each wallet variable, explain:

Its name (wallet, WALLET_S, WALLET_B, etc.).

Its role (e.g., “strategy wallet,” “sponsor wallet,” “fee wallet”).

How it is currently used (reads on‑chain data, sends transactions, receives fees).

Output this report clearly as “Wallet and DeFi Code Audit”, including:

File paths and function names.

Pseudocode summaries of critical flows (scraping, deposits, withdrawals, distributions).

Do not change any code in this step; just document.

B. Wallet model refactor (target architecture)
Based on the audit, refactor the code into a clear three‑wallet model:

walletUser – the connected consumer wallet (end user, comes from frontend connection).

walletS – existing “S” wallet from Replit secrets (kept for distribution logic).

walletB – existing “B” wallet from Replit secrets (if currently used; keep role intact).

Target behavior:

The old hard‑coded wallet is completely removed from business logic.

Wherever that wallet was used to read balances or supply to Aave, replace it with walletUser (the connected wallet address).

walletS and walletB remain environment‑based and are never exposed to the frontend.

Design and implement a wallet context / config module, for example server/walletConfig.ts, that exports:

Types:

ts
export type WalletRoles = {
  userAddress: string;   // runtime, provided per request
  walletS: string;       // from env
  walletB: string;       // from env (optional)
};
A factory function:

ts
export function getWalletRoles(userAddress: string): WalletRoles {...}
All DeFi / Aave functions should accept a WalletRoles object instead of reading from globals.

Example: initAaveForUser(wallets: WalletRoles) or rebalanceDefenses(wallets: WalletRoles, params: ...).

Update any distribution loop to clearly show:

From which wallet each transfer originates.

To which wallet(s) funds are sent.

Preserve existing percentages and destinations; just make the roles explicit.

Produce a diff‑style summary of the main refactors, including which functions changed signature and where walletUser flows into them.

C. Frontend wallet connection (consumer‑facing)
Implement or update the frontend to use only the user’s connected wallet for on‑chain actions:

Use Web3Modal + Ethers/Wagmi (or existing setup) to get connectedAddress.

On connection:

Send POST /api/auth/wallet with { walletAddress: connectedAddress }.

Backend upserts a users record and returns { userId, walletAddress }.

All subsequent API calls from the frontend send userId (and, if needed, the address) to identify the user.

Output:

File names and code snippets that implement this:

WalletConnectButton or equivalent.

Any React context/hook that stores userId and walletAddress (e.g., useCurrentUser).

D. Initial startup flow: stablecoin detection and Aave supply
Add an initial “startup” function that runs when a new user connects for the first time, or when their DeFi state is uninitialized.

Requirements:

After POST /api/auth/wallet, the backend should:

Check whether the user already has an active Aave profile/health record.

If not, trigger an initialization flow.

Create a dedicated module, e.g., server/aaveInit.ts, with a main function:

ts
export async function initializeUserDeFi(wallets: WalletRoles): Promise<{
  supplied: boolean;
  suppliedAmountUsd: number;
  healthFactor?: number;
  message: string;
}> {
  // 1) Detect stablecoins for walletUser
  // 2) Decide amount to supply (e.g., min threshold)
  // 3) Execute supply transactions
  // 4) Read health factor & save to DB
}
Implementation details:

Step 1: Find stablecoins

Check ERC20 balances for USDC/USDT/DAI for walletUser.

Use existing provider/Aave helpers; do NOT hard‑code RPC keys in code.

Step 2: Decide amount

Supply only above a minimum (e.g., > $50 equivalent).

Reserve a small on‑chain balance for gas.

Step 3: Supply to Aave

Use existing Aave integration functions where possible.

Associate resulting positions with the user in DB (defi_positions).

Step 4: Persist health

After supply, read health factor/LTV from Aave.

Save into defi_positions or a dedicated user_defi_state table.

Return values so the frontend can show a “Setup Complete” state.

Connect this to the API:

Extend /api/auth/wallet or add a new endpoint /api/defi/init.

Must return clear status so frontend can show:

“Scanning your wallet for stablecoins…”

“Supplying X USDC to Aave…”

“Health meter ready (HF = 1.3).”

Output:

New/updated file names: server/aaveInit.ts, any updated Aave helper files.

API endpoint definitions and sample JSON responses.

E. Data model and Replit SQL integration
Confirm or create the following tables in Replit SQL (Postgres):

users (wallet‑based identity).

towns, user_towns, filings (central scraping store).

defi_positions or user_defi_state.

income_events (for Personal Income).

If they don’t exist, add schema migrations or an init script.
Document in the output:

Exact SQL for new/modified tables.

Which backend modules read/write each table.

F. Outputs required for the redesign build
When you finish the audit and refactor, output all of the following so we can proceed to the UI build:

Wallet and DeFi Code Audit

File paths and function names where wallets are used.

Description of the old hard‑coded wallet and how it was replaced by walletUser.

Description of walletS and walletB roles and how they’re now encapsulated.

Module/File Map (for the new architecture)

Backend files:

server/server.ts (API entry)

server/db.ts

server/walletConfig.ts

server/aaveInit.ts

Any Aave/DeFi helper modules

server/scrape.ts for Lis Pendens scraping

Frontend files:

Wallet connect component & user context

API client

Placeholder dashboard components (even basic) that show how to call the new endpoints.

API Contract Documentation

For each endpoint (auth, towns, pipeline, system-health, defi, income, agent chat, defi init), provide:

HTTP method and path.

Request body/query.

Response JSON shape.

Initialization Flow Description

Step‑by‑step of what happens the moment a new user connects their wallet:

DB user creation.

Stablecoin scan.

Aave supply.

Health data persistence.

What data the frontend receives to display the first screen.

Any breaking changes

List any breaking changes to existing scripts, CRON/scheduled jobs, or environment variables.

Note any new secrets required